import{_ as i,C as d,o as t,c as r,a2 as o,b as s,w as a,a as n,E as l,a3 as p}from"./chunks/framework.C-cuuT8Z.js";const A=JSON.parse('{"title":"Tasks: Cortex Bootstrap Service","description":"","frontmatter":{},"headers":[],"relativePath":"002-cortex-setup/tasks.md","filePath":"002-cortex-setup/tasks.md","lastUpdated":1771785854000}'),u={name:"002-cortex-setup/tasks.md"};function h(m,e,g,T,f,S){const c=d("Mermaid");return t(),r("div",null,[e[1]||(e[1]=o(`<h1 id="tasks-cortex-bootstrap-service" tabindex="-1">Tasks: Cortex Bootstrap Service <a class="header-anchor" href="#tasks-cortex-bootstrap-service" aria-label="Permalink to &quot;Tasks: Cortex Bootstrap Service&quot;">​</a></h1><blockquote><p><strong>Spec</strong>: 002-cortex-setup <strong>Date</strong>: 2026-02-22</p></blockquote><h2 id="task-format" tabindex="-1">Task Format <a class="header-anchor" href="#task-format" aria-label="Permalink to &quot;Task Format&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[TASK-NNN] [P?] [MODULE] [PRIORITY] Description</span></span>
<span class="line"><span>  Dependencies: [TASK-XXX] or none</span></span>
<span class="line"><span>  Module: services/cortex/...</span></span>
<span class="line"><span>  Acceptance: Testable criteria</span></span>
<span class="line"><span>  Status: [ ] pending | [~] in-progress | [x] done</span></span></code></pre></div><ul><li><code>[P]</code> = Safe for parallel agent execution</li><li>Priority: P1 (must), P2 (should), P3 (nice)</li></ul><h2 id="dependency-graph" tabindex="-1">Dependency Graph <a class="header-anchor" href="#dependency-graph" aria-label="Permalink to &quot;Dependency Graph&quot;">​</a></h2>`,6)),(t(),s(p,null,{default:a(()=>[l(c,{id:"mermaid-27",class:"mermaid",graph:"graph%20TD%0A%20%20%20%20T001%20--%3E%20T010%0A%20%20%20%20T010%20--%3E%20T011%0A%20%20%20%20T011%20--%3E%20T012%0A%20%20%20%20T012%20--%3E%20T013%0A%0A%20%20%20%20T013%20--%3E%20T020%0A%20%20%20%20T013%20--%3E%20T021%0A%20%20%20%20T013%20--%3E%20T022%0A%20%20%20%20T013%20--%3E%20T023%0A%0A%20%20%20%20T020%20--%3E%20T030%0A%20%20%20%20T021%20--%3E%20T030%0A%20%20%20%20T022%20--%3E%20T030%0A%20%20%20%20T023%20--%3E%20T030%0A%0A%20%20%20%20T030%20--%3E%20T031%0A%0A%20%20%20%20T031%20--%3E%20T040%0A%20%20%20%20T031%20--%3E%20T041%0A%0A%20%20%20%20T040%20--%3E%20T900%0A%20%20%20%20T041%20--%3E%20T900%0A%20%20%20%20T900%20--%3E%20T999%0A%20%20%20%20T041%20--%3E%20T999%0A"})]),fallback:a(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=o('<h2 id="quality-requirements" tabindex="-1">Quality Requirements <a class="header-anchor" href="#quality-requirements" aria-label="Permalink to &quot;Quality Requirements&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Coverage</th><th>Lint</th><th>Notes</th></tr></thead><tbody><tr><td><code>internal/orchestrator</code></td><td>75%</td><td>golangci-lint</td><td>Critical — P1</td></tr><tr><td><code>internal/clients</code></td><td>75%</td><td>golangci-lint</td><td>Critical — P1</td></tr><tr><td><code>internal/api</code></td><td>60%</td><td>golangci-lint</td><td>Core — P1</td></tr><tr><td><code>internal/config</code></td><td>60%</td><td>golangci-lint</td><td>Core — P2</td></tr><tr><td><code>internal/telemetry</code></td><td>40%</td><td>golangci-lint</td><td>Infra — P2</td></tr></tbody></table><hr><h2 id="phase-1-setup" tabindex="-1">Phase 1: Setup <a class="header-anchor" href="#phase-1-setup" aria-label="Permalink to &quot;Phase 1: Setup&quot;">​</a></h2><p>&lt;!-- Module scaffold and Go module init — must be sequential, done by one agent --&gt;</p><ul><li><p>[x] [TASK-001] [SERVICES] [P1] Scaffold <code>services/cortex/</code> directory structure and <code>go.mod</code></p><ul><li>Dependencies: none</li><li>Module: <code>services/cortex/</code></li><li>Acceptance: All directories from spec exist; <code>go mod init arc-framework/cortex</code> succeeds; <code>go build ./...</code> compiles with no errors on an empty <code>main.go</code></li></ul></li><li><p>[x] [TASK-010] [SERVICES] [P1] Implement <code>internal/config/config.go</code> — Viper config with typed structs</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/cortex/internal/config/config.go</code></li><li>Acceptance: <code>Config</code> struct covers all four infra clients + OTEL + server; env var mapping matches <code>CORTEX_*</code> prefix; <code>LoadConfig()</code> returns typed struct; unit tests for defaults and env override pass</li></ul></li><li><p>[x] [TASK-011] [SERVICES] [P1] Implement <code>internal/telemetry/friday.go</code> — OTEL TracerProvider + MeterProvider</p><ul><li>Dependencies: TASK-010</li><li>Module: <code>services/cortex/internal/telemetry/friday.go</code></li><li>Acceptance: <code>InitProvider(ctx, cfg)</code> exports traces and metrics via OTLP gRPC to <code>arc-widow:4317</code>; shutdown is graceful; OTEL dial failure is non-fatal (<code>WithFailFast(false)</code>); unit test verifies provider initialises without panic when collector is unreachable</li></ul></li><li><p>[x] [TASK-012] [SERVICES] [P1] Implement Cobra CLI entry point — <code>cmd/cortex/</code> (root, server, bootstrap commands)</p><ul><li>Dependencies: TASK-010</li><li>Module: <code>services/cortex/cmd/cortex/</code></li><li>Acceptance: <code>cortex server</code> starts Gin on <code>:8081</code>; <code>cortex bootstrap</code> executes one-shot bootstrap and exits 0/non-zero; <code>--config</code>, <code>--log-level</code> global flags wired; <code>main.go</code> wires DI with functional options; compiles cleanly</li></ul></li><li><p>[x] [TASK-013] [SERVICES] [P1] Define orchestrator types — <code>internal/orchestrator/types.go</code></p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/cortex/internal/orchestrator/types.go</code></li><li>Acceptance: <code>BootstrapResult</code>, <code>PhaseResult</code>, <code>ProbeResult</code> structs defined; JSON tags present; <code>sync.Mutex</code> embedded in <code>BootstrapResult</code> for concurrent phase writes; zero-value is safe to use</li></ul></li></ul><hr><h2 id="phase-2-foundational-—-infra-clients-parallel-batch" tabindex="-1">Phase 2: Foundational — Infra Clients (Parallel Batch) <a class="header-anchor" href="#phase-2-foundational-—-infra-clients-parallel-batch" aria-label="Permalink to &quot;Phase 2: Foundational — Infra Clients (Parallel Batch)&quot;">​</a></h2><p>&lt;!-- All four clients are independent packages — run simultaneously with 4 agents --&gt;</p><h3 id="parallel-batch-a-—-4-agents-no-inter-dependency" tabindex="-1">Parallel Batch A — 4 agents, no inter-dependency <a class="header-anchor" href="#parallel-batch-a-—-4-agents-no-inter-dependency" aria-label="Permalink to &quot;Parallel Batch A — 4 agents, no inter-dependency&quot;">​</a></h3><ul><li><p>[x] [TASK-020] [P] [SERVICES] [P1] Implement <code>internal/clients/postgres.go</code> — pgx pool + schema validation + circuit breaker</p><ul><li>Dependencies: TASK-013</li><li>Module: <code>services/cortex/internal/clients/postgres.go</code></li><li>Acceptance: <code>NewPostgresClient(cfg, cb)</code> opens pgx pool; <code>Probe(ctx)</code> pings and validates schema version table exists; <code>gobreaker</code> trips after 3 failures, resets after 30s; table-driven tests with mocked pgx cover success, failure, and open-breaker cases; <code>golangci-lint</code> passes</li></ul></li><li><p>[x] [TASK-021] [P] [SERVICES] [P1] Implement <code>internal/clients/nats.go</code> — JetStream stream creation + circuit breaker</p><ul><li>Dependencies: TASK-013</li><li>Module: <code>services/cortex/internal/clients/nats.go</code></li><li>Acceptance: <code>NewNATSClient(cfg, cb)</code> connects via NATS; <code>ProvisionStreams(ctx)</code> creates 3 streams with correct subjects, retention, and max-age; existing stream is updated not errored; <code>gobreaker</code> circuit breaker applied; unit tests pass; <code>golangci-lint</code> passes</li><li>Note: Stream names use SCREAMING_SNAKE_CASE convention (<code>AGENT_COMMANDS</code>, <code>AGENT_EVENTS</code>, <code>SYSTEM_METRICS</code>) instead of the original spec placeholder names (<code>arc-commands</code>, <code>arc-events</code>, <code>arc-results</code>). NATS JetStream streams are conventionally uppercased; this is an intentional naming improvement.</li></ul></li><li><p>[x] [TASK-022] [P] [SERVICES] [P1] Implement <code>internal/clients/pulsar.go</code> — Pulsar admin REST + circuit breaker</p><ul><li>Dependencies: TASK-013</li><li>Module: <code>services/cortex/internal/clients/pulsar.go</code></li><li>Acceptance: <code>NewPulsarClient(cfg, cb)</code> uses <code>net/http</code> to Pulsar admin REST at <code>:8080</code>; provisions tenant <code>arc-system</code>, 3 namespaces, 3 partitioned topics; 409 on topic creation treated as success (idempotent); <code>gobreaker</code> applied; unit tests mock HTTP responses; <code>golangci-lint</code> passes</li></ul></li><li><p>[x] [TASK-023] [P] [SERVICES] [P1] Implement <code>internal/clients/redis.go</code> — go-redis PING + circuit breaker</p><ul><li>Dependencies: TASK-013</li><li>Module: <code>services/cortex/internal/clients/redis.go</code></li><li>Acceptance: <code>NewRedisClient(cfg, cb)</code> creates go-redis client; <code>Probe(ctx)</code> sends <code>PING</code> and validates <code>PONG</code>; <code>gobreaker</code> applied; unit tests cover success and circuit-open cases; <code>golangci-lint</code> passes</li></ul></li></ul><hr><h2 id="phase-3-implementation" tabindex="-1">Phase 3: Implementation <a class="header-anchor" href="#phase-3-implementation" aria-label="Permalink to &quot;Phase 3: Implementation&quot;">​</a></h2><p>&lt;!-- Orchestrator and API depend on all clients — sequential within this phase --&gt;</p><ul><li><p>[x] [TASK-030] [SERVICES] [P1] Implement <code>internal/orchestrator/service.go</code> — <code>RunBootstrap()</code> + <code>RunDeepHealth()</code></p><ul><li>Dependencies: TASK-020, TASK-021, TASK-022, TASK-023</li><li>Module: <code>services/cortex/internal/orchestrator/service.go</code></li><li>Acceptance: <code>RunBootstrap(ctx)</code> runs all 4 phases via <code>errgroup</code> in parallel; each phase writes to <code>BootstrapResult</code> under mutex; phase error does not cancel others; <code>atomic.Bool</code> gates concurrent bootstrap requests (returns sentinel error when in-progress); OTEL span wraps full execution; table-driven tests achieve ≥ 75% coverage; <code>golangci-lint</code> passes</li></ul></li><li><p>[x] [TASK-031] [SERVICES] [P1] Implement <code>internal/api/</code> — Gin router, handlers, and OTEL middleware</p><ul><li>Dependencies: TASK-030</li><li>Module: <code>services/cortex/internal/api/</code></li><li>Acceptance: <code>POST /api/v1/bootstrap</code> returns 202 on start, 409 if in-progress; <code>GET /health</code> always 200; <code>GET /health/deep</code> probes all 4 clients; <code>GET /ready</code> returns 503 until bootstrap completes, then 200; OTEL trace propagation middleware applied; panic recovery middleware present; ≥ 60% test coverage on handlers; <code>golangci-lint</code> passes</li></ul></li></ul><hr><h2 id="phase-4-integration" tabindex="-1">Phase 4: Integration <a class="header-anchor" href="#phase-4-integration" aria-label="Permalink to &quot;Phase 4: Integration&quot;">​</a></h2><p>&lt;!-- Wire everything together and validate end-to-end --&gt;</p><ul><li><p>[x] [TASK-040] [SERVICES] [P1] Wire <code>main.go</code> DI + integration test for bootstrap flow</p><ul><li>Dependencies: TASK-031</li><li>Module: <code>services/cortex/cmd/cortex/main.go</code></li><li>Acceptance: <code>main.go</code> constructs all dependencies via functional options and wires them into the Cobra command tree; <code>go build ./...</code> produces a valid binary; integration test spins up Gin with mock clients and verifies full <code>POST /api/v1/bootstrap → 202 → GET /ready → 200</code> flow; <code>golangci-lint</code> passes</li></ul></li><li><p>[x] [TASK-041] [P] [SERVICES] [P1] Add <code>service.yaml</code> and <code>Dockerfile</code> for Cortex</p><ul><li>Dependencies: TASK-031</li><li>Module: <code>services/cortex/</code></li><li>Acceptance: <code>service.yaml</code> declares <code>codename: cortex</code>, <code>depends_on: [flash, strange, oracle, sonic, widow]</code>, health endpoint <code>/health</code>; Dockerfile uses multi-stage build, non-root <code>USER cortex</code>, and exposes <code>:8081</code>; <code>docker build</code> succeeds; <code>docker inspect</code> confirms non-root user</li></ul></li></ul><hr><h2 id="phase-5-polish" tabindex="-1">Phase 5: Polish <a class="header-anchor" href="#phase-5-polish" aria-label="Permalink to &quot;Phase 5: Polish&quot;">​</a></h2><p>&lt;!-- Docs, config updates, cross-links — can overlap with Phase 4 --&gt;</p><ul><li><p>[x] [TASK-900] [P] [DOCS] [P1] Update platform references — profiles.yaml, config.yaml, codename map, architecture doc</p><ul><li>Dependencies: TASK-040, TASK-041</li><li>Module: <code>services/profiles.yaml</code>, <code>.specify/config.yaml</code>, <code>.specify/meta/service-codename-map.md</code>, <code>.specify/docs/architecture/cortex.md</code></li><li>Acceptance: <code>cortex</code> added to <code>think</code> and <code>reason</code> profiles in <code>profiles.yaml</code>; <code>.specify/config.yaml</code> updated from <code>bootstrap/raymond</code> → <code>cortex/cortex</code>; <code>service-codename-map.md</code> has Cortex row; <code>cortex.md</code> architecture doc links back to <code>spec.md</code> and <code>plan.md</code>; all internal links resolve</li></ul></li><li><p>[x] [TASK-999] [REVIEW] [P1] Reviewer agent verification — all tasks complete, tests pass, constitution compliant</p><ul><li>Dependencies: ALL</li><li>Module: all affected modules</li><li>Acceptance: All tasks marked <code>[x]</code>; <code>go test ./...</code> passes with ≥ 75% on orchestrator+clients, ≥ 60% on api; <code>golangci-lint run</code> zero errors; <code>GET /health</code> responds &lt; 50ms; <code>docker inspect</code> confirms non-root user; OTEL service name is <code>arc-cortex</code>; <code>POST /api/v1/bootstrap</code> returns 409 when in-progress; <code>cortex bootstrap</code> exits 0 on success; constitution principles II, III, IV, V, VII, VIII, XI all verified</li></ul></li></ul><hr><h2 id="progress-summary" tabindex="-1">Progress Summary <a class="header-anchor" href="#progress-summary" aria-label="Permalink to &quot;Progress Summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Phase</th><th>Total</th><th>Done</th><th>Parallel</th></tr></thead><tbody><tr><td>Setup</td><td>5</td><td>5</td><td>0</td></tr><tr><td>Foundational (Clients)</td><td>4</td><td>4</td><td>4</td></tr><tr><td>Implementation</td><td>2</td><td>2</td><td>0</td></tr><tr><td>Integration</td><td>2</td><td>2</td><td>1</td></tr><tr><td>Polish</td><td>2</td><td>2</td><td>1</td></tr><tr><td><strong>Total</strong></td><td><strong>15</strong></td><td><strong>15</strong></td><td><strong>6</strong></td></tr></tbody></table>',26))])}const b=i(u,[["render",h]]);export{A as __pageData,b as default};
