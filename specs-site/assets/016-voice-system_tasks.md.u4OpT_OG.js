import{_ as r,C as s,o,c as a,an as c,b as i,w as t,a as n,E as l,ao as p}from"./chunks/framework.CTqRDTHW.js";const S=JSON.parse('{"title":"Tasks: Voice System","description":"","frontmatter":{},"headers":[],"relativePath":"016-voice-system/tasks.md","filePath":"016-voice-system/tasks.md","lastUpdated":1773141627000}'),u={name:"016-voice-system/tasks.md"};function h(T,e,m,v,y,A){const d=s("Mermaid");return o(),a("div",null,[e[1]||(e[1]=c(`<h1 id="tasks-voice-system" tabindex="-1">Tasks: Voice System <a class="header-anchor" href="#tasks-voice-system" aria-label="Permalink to &quot;Tasks: Voice System&quot;">​</a></h1><blockquote><p><strong>Spec</strong>: 016-voice-system <strong>Date</strong>: 2026-03-07</p></blockquote><h2 id="task-format" tabindex="-1">Task Format <a class="header-anchor" href="#task-format" aria-label="Permalink to &quot;Task Format&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[TASK-NNN] [P?] [MODULE] [PRIORITY] Description</span></span>
<span class="line"><span>  Dependencies: [TASK-XXX] or none</span></span>
<span class="line"><span>  Module: services/voice/...</span></span>
<span class="line"><span>  Acceptance: Testable criteria</span></span>
<span class="line"><span>  Status: [ ] pending | [~] in-progress | [x] done</span></span></code></pre></div><ul><li><code>[P]</code> = Safe for parallel agent execution</li><li>Priority: P1 (must), P2 (should), P3 (nice)</li></ul><h2 id="dependency-graph" tabindex="-1">Dependency Graph <a class="header-anchor" href="#dependency-graph" aria-label="Permalink to &quot;Dependency Graph&quot;">​</a></h2>`,6)),(o(),i(p,null,{default:t(()=>[l(d,{id:"mermaid-27",class:"mermaid",graph:"graph%20TD%0A%20%20%20%20T001%5B%22TASK-001%5CnScaffold%20service%20dir%22%5D%20--%3E%20T010%0A%20%20%20%20T001%20--%3E%20T011%0A%20%20%20%20T001%20--%3E%20T012%0A%0A%20%20%20%20T010%5B%22TASK-010%5Cnconfig%20%2B%20settings%22%5D%20--%3E%20T020%0A%20%20%20%20T010%20--%3E%20T021%0A%20%20%20%20T010%20--%3E%20T022%0A%20%20%20%20T010%20--%3E%20T023%0A%0A%20%20%20%20T011%5B%22TASK-011%5Cninterfaces.py%5Cn(STTPort%2C%20TTSPort%2C%20LLMBridgePort)%22%5D%20--%3E%20T020%0A%20%20%20%20T011%20--%3E%20T021%0A%20%20%20%20T011%20--%3E%20T022%0A%20%20%20%20T011%20--%3E%20T023%0A%20%20%20%20T011%20--%3E%20T030%0A%0A%20%20%20%20T012%5B%22TASK-012%5Cnobservability.py%5Cn(OTEL%20tracer%2Fmeter%2Fhistograms)%22%5D%20--%3E%20T020%0A%20%20%20%20T012%20--%3E%20T021%0A%20%20%20%20T012%20--%3E%20T022%0A%20%20%20%20T012%20--%3E%20T023%0A%20%20%20%20T012%20--%3E%20T030%0A%0A%20%20%20%20T020%5B%22TASK-020%20%5BP%5D%5CnSTT%20Whisper%20adapter%20%2B%20router%22%5D%20--%3E%20T040%0A%20%20%20%20T021%5B%22TASK-021%20%5BP%5D%5CnTTS%20Piper%20adapter%20%2B%20router%22%5D%20--%3E%20T040%0A%20%20%20%20T022%5B%22TASK-022%20%5BP%5D%5CnHealth%20router%22%5D%20--%3E%20T040%0A%20%20%20%20T023%5B%22TASK-023%20%5BP%5D%5Cnmodels_v1.py%5Cn(Pydantic%20models%20%2B%20events)%22%5D%20--%3E%20T020%0A%20%20%20%20T023%20--%3E%20T021%0A%20%20%20%20T023%20--%3E%20T030%0A%20%20%20%20T023%20--%3E%20T031%0A%0A%20%20%20%20T030%5B%22TASK-030%20%5BP%5D%5CnNATS%20bridge%5Cn(LLMBridgePort%20impl)%22%5D%20--%3E%20T040%0A%20%20%20%20T031%5B%22TASK-031%20%5BP%5D%5CnPulsar%20event%20publisher%22%5D%20--%3E%20T040%0A%0A%20%20%20%20T040%5B%22TASK-040%5CnLiveKit%20worker%5Cn(room%20handler)%22%5D%20--%3E%20T050%0A%20%20%20%20T040%20--%3E%20T041%5B%22TASK-041%20%5BP%5D%5CnLoad%2Flatency%20test%5Cn(750-1550%20ms%20SLO)%22%5D%0A%20%20%20%20T041%20--%3E%20T999%0A%20%20%20%20T050%5B%22TASK-050%5Cnmain.py%20wiring%5Cn%2B%20lifespan%22%5D%20--%3E%20T060%0A%0A%20%20%20%20T060%5B%22TASK-060%5CnContracts%5Cn(openapi%20%2B%20asyncapi)%22%5D%20--%3E%20T070%0A%20%20%20%20T070%5B%22TASK-070%5CnDocker%20%2B%20compose%5Cn%2B%20profiles.yaml%22%5D%20--%3E%20T080%0A%20%20%20%20T080%5B%22TASK-080%5CnCI%20workflow%22%5D%20--%3E%20T900%0A%0A%20%20%20%20T900%5B%22TASK-900%20%5BP%5D%5CnDocs%20update%22%5D%20--%3E%20T999%0A%20%20%20%20T080%20--%3E%20T999%0A%20%20%20%20T900%20--%3E%20T999%0A%20%20%20%20T999%5B%22TASK-999%5CnReviewer%20verification%22%5D%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=c('<h2 id="quality-requirements" tabindex="-1">Quality Requirements <a class="header-anchor" href="#quality-requirements" aria-label="Permalink to &quot;Quality Requirements&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Coverage</th><th>Lint</th></tr></thead><tbody><tr><td><code>services/voice/</code> (Python)</td><td>75% critical / 60% core / 40% infra</td><td><code>ruff</code> + <code>mypy</code></td></tr></tbody></table><hr><h2 id="phase-1-setup" tabindex="-1">Phase 1: Setup <a class="header-anchor" href="#phase-1-setup" aria-label="Permalink to &quot;Phase 1: Setup&quot;">​</a></h2><ul><li>[x] [TASK-001] [VOICE] [P1] Create <code>services/voice/</code> directory structure with service.yaml, Dockerfile, pyproject.toml, uv.lock placeholder, and voice.mk <ul><li>Dependencies: none</li><li>Module: <code>services/voice/</code></li><li>Acceptance: <code>services/voice/service.yaml</code> declares <code>name: arc-voice-agent</code>, <code>codename: scarlett</code>, <code>port: 8803</code>, correct <code>depends_on</code> (messaging, streaming, realtime, friday-collector, cache); Dockerfile uses non-root user, mirrors reasoner multi-stage build, and installs <code>piper</code> binary to <code>/usr/local/bin/piper</code>; <code>pyproject.toml</code> declares <code>arc-voice-agent</code> package with fastapi, uvicorn, pydantic-settings, livekit-agents, faster-whisper, piper-tts, nats-py, <code>pulsar-client==3.10.0</code> (matches <code>services/reasoner/uv.lock</code> — cp313 wheels verified for all target platforms), opentelemetry-api/sdk deps; <code>uv lock</code> runs cleanly</li></ul></li></ul><hr><h2 id="phase-2-foundational" tabindex="-1">Phase 2: Foundational <a class="header-anchor" href="#phase-2-foundational" aria-label="Permalink to &quot;Phase 2: Foundational&quot;">​</a></h2><ul><li><p>[x] [TASK-010] [P] [VOICE] [P1] Implement <code>src/voice/config.py</code> — pydantic-settings <code>Settings</code> class with all env vars</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/voice/src/voice/config.py</code></li><li>Acceptance: <code>Settings</code> covers <code>VOICE_STT_PROVIDER</code>, <code>VOICE_TTS_PROVIDER</code>, <code>VOICE_BRIDGE_NATS_SUBJECT</code> (default <code>arc.reasoner.request</code> — matches Spec 015 final state; see <code>services/reasoner/src/reasoner/nats_handler.py</code>), <code>VOICE_BRIDGE_TIMEOUT_MS</code> (default <code>10000</code>), <code>VOICE_LIVEKIT_URL</code>, <code>VOICE_LIVEKIT_API_KEY</code>, <code>VOICE_LIVEKIT_API_SECRET</code>, <code>VOICE_OTEL_ENDPOINT</code>, <code>VOICE_PORT</code> (default 8803), <code>VOICE_LOG_LEVEL</code>; all vars have sane defaults; <code>Settings()</code> instantiates with no env vars set</li></ul></li><li><p>[x] [TASK-011] [P] [VOICE] [P1] Implement <code>src/voice/interfaces.py</code> — <code>STTPort</code>, <code>TTSPort</code>, <code>LLMBridgePort</code> Protocol interfaces + result dataclasses</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/voice/src/voice/interfaces.py</code></li><li>Acceptance: <code>STTPort.transcribe(audio_bytes, language) -&gt; TranscriptResult</code> defined; <code>TTSPort.synthesize(text, voice) -&gt; SynthesisResult</code> defined; <code>LLMBridgePort.reason(transcript, session_id, correlation_id) -&gt; str</code> defined; <code>TranscriptResult</code> has <code>text</code>, <code>language</code>, <code>duration_secs</code>; <code>SynthesisResult</code> has <code>wav_bytes</code>, <code>sample_rate</code>, <code>duration_secs</code>; <code>mypy</code> passes on the file</li></ul></li><li><p>[x] [TASK-012] [P] [VOICE] [P1] Implement <code>src/voice/observability.py</code> — OTEL tracer, meter, and per-stage histograms</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/voice/src/voice/observability.py</code></li><li>Acceptance: <code>setup_telemetry(settings)</code> configures OTEL exporter and returns tracer + meter; histograms defined: <code>voice.stt.latency_seconds</code>, <code>voice.tts.latency_seconds</code>, <code>voice.bridge.latency_seconds</code>, <code>voice.turn.latency_seconds</code>; <code>get_tracer()</code> / <code>get_meter()</code> accessors exported; works with no OTEL endpoint (no-op exporter fallback)</li></ul></li><li><p>[x] [TASK-023] [P] [VOICE] [P1] Implement <code>src/voice/models_v1.py</code> — Pydantic I/O models and event schemas</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/voice/src/voice/models_v1.py</code></li><li>Acceptance: <code>TranscriptionRequest</code>, <code>TranscriptionResponse</code> (OpenAI-compatible: <code>text</code>, <code>language</code>, <code>duration</code>); <code>SpeechRequest</code> (<code>model</code>, <code>input</code>, <code>voice</code>, <code>response_format</code>); <code>VoiceSessionStartedEvent</code>, <code>VoiceSessionEndedEvent</code>, <code>VoiceTurnCompletedEvent</code>, <code>VoiceTurnFailedEvent</code> with <code>session_id</code>, <code>room_id</code>, <code>correlation_id</code>, <code>timestamp</code>, latency/duration fields; no secrets or audio payload fields; <code>mypy</code> passes</li><li><strong>GAP-1</strong> <code>ErrorResponse(error_type: Literal[&#39;unsupported_format&#39;, &#39;provider_unavailable&#39;, &#39;invalid_input&#39;], message: str, correlation_id: str)</code> — used by STT and TTS routers for 400/502 responses</li><li><strong>GAP-2</strong> <code>HealthCheckDetail(status: Literal[&#39;ok&#39;, &#39;degraded&#39;, &#39;unhealthy&#39;], reason: str | None, latency_ms: float | None)</code> and <code>HealthCheckResponse(status: Literal[&#39;ok&#39;, &#39;degraded&#39;, &#39;unhealthy&#39;], checks: dict[str, HealthCheckDetail])</code> — used by <code>/health/deep</code></li></ul></li></ul><hr><h2 id="phase-3-implementation" tabindex="-1">Phase 3: Implementation <a class="header-anchor" href="#phase-3-implementation" aria-label="Permalink to &quot;Phase 3: Implementation&quot;">​</a></h2><h3 id="parallel-batch-a-—-rest-handlers" tabindex="-1">Parallel Batch A — REST Handlers <a class="header-anchor" href="#parallel-batch-a-—-rest-handlers" aria-label="Permalink to &quot;Parallel Batch A — REST Handlers&quot;">​</a></h3><ul><li><p>[x] [TASK-020] [P] [VOICE] [P1] Implement faster-whisper STT adapter and <code>POST /v1/audio/transcriptions</code> router</p><ul><li>Dependencies: TASK-010, TASK-011, TASK-012, TASK-023</li><li>Module: <code>services/voice/src/voice/providers/stt_whisper.py</code>, <code>services/voice/src/voice/stt_router.py</code>, <code>tests/test_stt_router.py</code>, <code>tests/test_providers/test_stt_whisper.py</code></li><li>Acceptance: <code>WhisperSTTAdapter</code> implements <code>STTPort</code>; <code>POST /v1/audio/transcriptions</code> accepts multipart <code>file</code> field; returns JSON <code>{text, language, duration}</code>; rejects unsupported MIME types with typed 400; provider failure returns typed 502; OTEL span wraps transcription; tests pass with mocked Whisper model; <code>ruff</code> + <code>mypy</code> clean</li></ul></li><li><p>[x] [TASK-021] [P] [VOICE] [P1] Implement piper TTS adapter and <code>POST /v1/audio/speech</code> router</p><ul><li>Dependencies: TASK-010, TASK-011, TASK-012, TASK-023</li><li>Module: <code>services/voice/src/voice/providers/tts_piper.py</code>, <code>services/voice/src/voice/tts_router.py</code>, <code>tests/test_tts_router.py</code>, <code>tests/test_providers/test_tts_piper.py</code></li><li>Acceptance: <code>PiperTTSAdapter</code> implements <code>TTSPort</code>; <code>POST /v1/audio/speech</code> accepts JSON <code>{input, voice, model}</code>; returns <code>StreamingResponse</code> with <code>Content-Type: audio/wav</code>, <code>X-Duration-Seconds</code>, <code>X-Sample-Rate</code> headers; empty <code>input</code> returns 400; provider failure returns typed 502; OTEL span wraps synthesis; tests pass with mocked piper binary; <code>ruff</code> + <code>mypy</code> clean</li></ul></li><li><p>[x] [TASK-022] [P] [VOICE] [P1] Implement <code>GET /health</code> and <code>GET /health/deep</code> router</p><ul><li>Dependencies: TASK-010, TASK-012</li><li>Module: <code>services/voice/src/voice/health_router.py</code>, <code>tests/test_health_router.py</code></li><li>Acceptance: <code>GET /health</code> returns <code>{status: &quot;ok&quot;}</code> 200 when process is alive (no deps checked); <code>GET /health/deep</code> checks <code>arc-realtime</code> connectivity (LiveKit API ping) and <code>arc-messaging</code> connectivity (NATS ping); returns <code>{status: &quot;degraded&quot;, checks: {...}}</code> with 200 when deps down (not 500); individual check failures report reason; tests cover all combinations; <code>ruff</code> + <code>mypy</code> clean</li></ul></li></ul><h3 id="parallel-batch-b-—-messaging" tabindex="-1">Parallel Batch B — Messaging <a class="header-anchor" href="#parallel-batch-b-—-messaging" aria-label="Permalink to &quot;Parallel Batch B — Messaging&quot;">​</a></h3><ul><li><p>[x] [TASK-030] [P] [VOICE] [P1] Implement NATS bridge (<code>LLMBridgePort</code> impl) for reasoner request-reply</p><ul><li>Dependencies: TASK-011, TASK-012, TASK-023</li><li>Module: <code>services/voice/src/voice/nats_bridge.py</code>, <code>tests/test_nats_bridge.py</code></li><li>Acceptance: <code>NATSBridge</code> implements <code>LLMBridgePort</code>; publishes to <code>VOICE_BRIDGE_NATS_SUBJECT</code> with <code>request_id</code> correlation; awaits <code>arc.reasoner.result</code> reply with configurable timeout; on timeout or <code>arc.reasoner.error</code> raises typed <code>BridgeError</code>; OTEL span wraps the round-trip; <code>_nc</code> connection shared via lifespan; tests use mock NATS server or <code>AsyncMock</code>; <code>ruff</code> + <code>mypy</code> clean</li></ul></li><li><p>[x] [TASK-031] [P] [VOICE] [P1] Implement Pulsar event publisher (fire-and-forget) for voice lifecycle events</p><ul><li>Dependencies: TASK-023</li><li>Module: <code>services/voice/src/voice/pulsar_events.py</code>, <code>tests/test_pulsar_events.py</code></li><li>Acceptance: <code>VoiceEventPublisher</code> wraps Pulsar client; exposes <code>publish_session_started</code>, <code>publish_session_ended</code>, <code>publish_turn_completed</code>, <code>publish_turn_failed</code>; each wraps <code>asyncio.create_task()</code> so publishing never blocks caller; Pulsar unavailability logs warning and returns without raising; event payloads match <code>models_v1.py</code> schemas; no secrets or audio bytes in payloads; tests verify fire-and-forget behaviour (await <code>asyncio.sleep(0)</code> to drain); <code>ruff</code> + <code>mypy</code> clean</li></ul></li></ul><hr><h2 id="phase-4-integration" tabindex="-1">Phase 4: Integration <a class="header-anchor" href="#phase-4-integration" aria-label="Permalink to &quot;Phase 4: Integration&quot;">​</a></h2><ul><li><p>[x] [TASK-040] [VOICE] [P1] Implement LiveKit worker — VAD → STT → NATSBridge → TTS → room audio</p><ul><li>Dependencies: TASK-020, TASK-021, TASK-030, TASK-031</li><li>Module: <code>services/voice/src/voice/worker.py</code>, <code>tests/test_worker.py</code></li><li>Acceptance: <code>VoiceWorker</code> connects to LiveKit room as agent; VAD detects end-of-speech; calls <code>STTPort.transcribe()</code>; calls <code>LLMBridgePort.reason()</code>; calls <code>TTSPort.synthesize()</code>; publishes audio frames back to room; publishes <code>VoiceTurnCompletedEvent</code> on success and <code>VoiceTurnFailedEvent</code> on any exception; OTEL turn span wraps full pipeline; room failure publishes failure event and logs without crashing worker loop; tests mock LiveKit room and all ports; <code>ruff</code> + <code>mypy</code> clean</li><li><strong>WARN-3</strong> Exception → <code>error_type</code> mapping for <code>VoiceTurnFailedEvent</code>: <code>STTError</code> → <code>&#39;stt_error&#39;</code>; <code>BridgeError</code> with timeout → <code>&#39;bridge_timeout&#39;</code>; <code>BridgeError</code> with reasoner error → <code>&#39;bridge_error&#39;</code>; <code>TTSError</code> → <code>&#39;tts_error&#39;</code>; any other unhandled exception → <code>&#39;unknown&#39;</code>; <code>error_type</code> must be a <code>Literal</code> field in <code>VoiceTurnFailedEvent</code> (defined in TASK-023)</li></ul></li><li><p>[x] [TASK-041] [P] [VOICE] [P2] Load and latency validation — confirm end-to-end turn budget (750–1550 ms) under concurrent rooms</p><ul><li>Dependencies: TASK-040</li><li>Module: <code>tests/test_latency.py</code> (or <code>tests/test_load.py</code>)</li><li>Acceptance: Pytest benchmark or <code>locust</code> script simulates ≥3 concurrent room turns with mocked STT/TTS/NATS providers returning fixed-latency stubs; asserts p95 end-to-end turn latency ≤ 1550 ms across 50 iterations; OTEL histogram <code>voice.turn.latency_seconds</code> records each measurement; results logged to stdout for CI visibility; test is skipped (<code>pytest.mark.skip</code>) in short mode via <code>--no-load-test</code> flag</li></ul></li><li><p>[x] [TASK-050] [VOICE] [P1] Implement <code>src/voice/main.py</code> — FastAPI app factory, lifespan, router registration, worker launch</p><ul><li>Dependencies: TASK-040, TASK-022</li><li>Module: <code>services/voice/src/voice/main.py</code></li><li>Acceptance: <code>create_app()</code> factory wires routers (STT, TTS, health); lifespan starts NATS connection, Pulsar client, OTEL, then launches <code>VoiceWorker</code> as <code>asyncio.create_task()</code>; lifespan teardown cancels <code>VoiceWorker</code> task with <strong>5 s timeout</strong> (log error and continue if exceeded), then closes Pulsar client, then closes NATS connection (shutdown order: worker → Pulsar → NATS); integration test: pytest fixture boots the full app with <code>httpx.AsyncClient(app=create_app(), ..., lifespan=&#39;on&#39;)</code> and confirms clean startup + teardown without deadlock; <code>uvicorn main:create_app()</code> starts without error with only local env vars set; <code>ruff</code> + <code>mypy</code> clean</li></ul></li></ul><hr><h2 id="phase-5-contracts-infrastructure" tabindex="-1">Phase 5: Contracts &amp; Infrastructure <a class="header-anchor" href="#phase-5-contracts-infrastructure" aria-label="Permalink to &quot;Phase 5: Contracts &amp; Infrastructure&quot;">​</a></h2><ul><li><p>[x] [TASK-060] [P] [VOICE] [P1] Write <code>contracts/openapi.yaml</code> and <code>contracts/asyncapi.yaml</code></p><ul><li>Dependencies: TASK-050</li><li>Module: <code>services/voice/contracts/</code></li><li>Acceptance: <code>openapi.yaml</code> documents <code>/v1/audio/transcriptions</code>, <code>/v1/audio/speech</code>, <code>/health</code>, <code>/health/deep</code> with request/response schemas matching <code>models_v1.py</code>; <code>asyncapi.yaml</code> documents <code>arc.voice.session.started</code>, <code>arc.voice.session.ended</code>, <code>arc.voice.turn.completed</code>, <code>arc.voice.turn.failed</code> topics with full payload schemas; both files are valid YAML; schemas match what the service actually produces</li></ul></li><li><p>[x] [TASK-070] [P] [VOICE] [P1] Add docker-compose.yml, update <code>services/profiles.yaml</code> to add <code>voice</code> to <code>reason</code> profile</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/voice/docker-compose.yml</code>, <code>services/profiles.yaml</code></li><li>Acceptance: <code>services/voice/docker-compose.yml</code> defines <code>arc-voice-agent</code> service with correct env vars, port (8803), <code>depends_on</code> (arc-realtime, arc-messaging, arc-streaming, arc-friday-collector, arc-cache), and health check; <code>services/profiles.yaml</code> <code>reason</code> profile includes <code>voice</code>; <code>ultra-instinct</code> profile still resolves correctly (uses <code>*</code>); <code>make dev PROFILE=reason</code> starts without compose errors</li></ul></li><li><p>[x] [TASK-080] [P] [VOICE] [P2] Add CI workflow for voice image build, test, and release</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>.github/workflows/voice.yml</code></li><li>Acceptance: Workflow triggers on push to <code>services/voice/**</code> and tags <code>voice/v*</code>; runs <code>uv run ruff check src/</code>, <code>uv run mypy src/</code>, <code>uv run python -m pytest tests/ -q</code>; builds and pushes <code>ghcr.io/arc-framework/arc-voice-agent</code> on tag; mirrors structure of existing service workflows</li></ul></li></ul><hr><h2 id="phase-6-polish" tabindex="-1">Phase 6: Polish <a class="header-anchor" href="#phase-6-polish" aria-label="Permalink to &quot;Phase 6: Polish&quot;">​</a></h2><ul><li><p>[x] [TASK-900] [P] [DOCS] [P1] Create <code>docs/ard/VOICE-SYSTEM.md</code>, update <code>docs/ard/VOICE-HLD.md</code>, and link to framework doc</p><ul><li>Dependencies: TASK-060</li><li>Module: <code>docs/ard/</code></li><li>Acceptance: <code>VOICE-SYSTEM.md</code> documents service codename (Scarlett), ports (8803), dependencies, event topics, local provider defaults, and latency budget; <code>VOICE-HLD.md</code> updated with architecture diagram reflecting final implementation; <code>docs/ard/ARC-ENTERPRISE-AI-FRAMEWORK.md</code> links to voice system doc; all internal links resolve; no broken references</li></ul></li><li><p>[x] [TASK-999] [REVIEW] [P1] Reviewer agent verification — validate all tasks complete, quality gates met</p><ul><li>Dependencies: ALL</li><li>Module: all (<code>services/voice/</code>, <code>services/profiles.yaml</code>, <code>.github/workflows/</code>, <code>docs/ard/</code>)</li><li>Acceptance: <ul><li>All TASK-001 through TASK-900 marked <code>[x]</code> done</li><li><code>uv run python -m pytest tests/ -q</code> passes (zero failures) in <code>services/voice/</code></li><li><code>uv run ruff check src/</code> passes (zero warnings)</li><li><code>uv run mypy src/</code> passes (no errors)</li><li><code>GET /health</code> returns 200; <code>GET /health/deep</code> returns 200 or degraded (not 500) with realtime down</li><li><code>POST /v1/audio/transcriptions</code> returns valid JSON with transcript + language + duration</li><li><code>POST /v1/audio/speech</code> returns WAV stream with correct headers</li><li><code>voice</code> present in <code>reason</code> profile in <code>profiles.yaml</code></li><li><code>service.yaml</code> has non-root Dockerfile, correct health check, correct depends_on</li><li><code>contracts/openapi.yaml</code> and <code>contracts/asyncapi.yaml</code> are valid and consistent with implementation</li><li>No secrets, raw audio, or PII in any log output or event payload</li><li>Constitution II WARNING documented (reason-profile-only, model footprint)</li><li><strong>GAP-3</strong> Per-module coverage targets met: <code>interfaces.py</code> ≥95%, <code>nats_bridge.py</code> ≥90%, <code>worker.py</code> ≥85%, <code>health_router.py</code> ≥80%, <code>stt_router.py</code> + <code>tts_router.py</code> ≥80%, <code>providers/*</code> ≥75%, <code>pulsar_events.py</code> ≥75%</li></ul></li></ul></li></ul><hr><h2 id="progress-summary" tabindex="-1">Progress Summary <a class="header-anchor" href="#progress-summary" aria-label="Permalink to &quot;Progress Summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Phase</th><th>Total</th><th>Done</th><th>Parallel</th></tr></thead><tbody><tr><td>Setup</td><td>1</td><td>0</td><td>0</td></tr><tr><td>Foundational</td><td>4</td><td>0</td><td>4</td></tr><tr><td>Implementation</td><td>5</td><td>0</td><td>5</td></tr><tr><td>Integration</td><td>3</td><td>0</td><td>1</td></tr><tr><td>Contracts &amp; Infra</td><td>3</td><td>0</td><td>3</td></tr><tr><td>Polish</td><td>2</td><td>0</td><td>1</td></tr><tr><td><strong>Total</strong></td><td><strong>18</strong></td><td><strong>0</strong></td><td><strong>14</strong></td></tr></tbody></table>',26))])}const f=r(u,[["render",h]]);export{S as __pageData,f as default};
