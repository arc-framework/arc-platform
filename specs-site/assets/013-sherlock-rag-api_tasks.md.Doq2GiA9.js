import{_ as s,C as r,o,c as a,an as c,b as i,w as t,a as n,E as l,ao as p}from"./chunks/framework.CTqRDTHW.js";const k=JSON.parse('{"title":"Tasks: Sherlock Universal RAG API","description":"","frontmatter":{},"headers":[],"relativePath":"013-sherlock-rag-api/tasks.md","filePath":"013-sherlock-rag-api/tasks.md","lastUpdated":1772696547000}'),u={name:"013-sherlock-rag-api/tasks.md"};function h(m,e,A,g,T,y){const d=r("Mermaid");return o(),a("div",null,[e[1]||(e[1]=c(`<h1 id="tasks-sherlock-universal-rag-api" tabindex="-1">Tasks: Sherlock Universal RAG API <a class="header-anchor" href="#tasks-sherlock-universal-rag-api" aria-label="Permalink to &quot;Tasks: Sherlock Universal RAG API&quot;">​</a></h1><blockquote><p><strong>Spec</strong>: 013-sherlock-rag-api <strong>Date</strong>: 2026-03-03</p></blockquote><h2 id="task-format" tabindex="-1">Task Format <a class="header-anchor" href="#task-format" aria-label="Permalink to &quot;Task Format&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[TASK-NNN] [P?] [MODULE] [PRIORITY] Description</span></span>
<span class="line"><span>  Dependencies: [TASK-XXX] or none</span></span>
<span class="line"><span>  Module: path</span></span>
<span class="line"><span>  Acceptance: Testable criteria</span></span>
<span class="line"><span>  Status: [ ] pending | [~] in-progress | [x] done</span></span></code></pre></div><ul><li><code>[P]</code> = Safe for parallel agent execution</li><li>Priority: P1 (must), P2 (should), P3 (nice)</li></ul><hr><h2 id="dependency-graph" tabindex="-1">Dependency Graph <a class="header-anchor" href="#dependency-graph" aria-label="Permalink to &quot;Dependency Graph&quot;">​</a></h2>`,7)),(o(),i(p,null,{default:t(()=>[l(d,{id:"mermaid-28",class:"mermaid",graph:"graph%20TD%0A%20%20%20%20T001%5B%22001%20config.py%20additions%22%5D%0A%20%20%20%20T002%5B%22002%20DB%20schema%20SQL%22%5D%0A%20%20%20%20T003%5B%22003%20pyproject.toml%20deps%22%5D%0A%20%20%20%20T004%5B%22004%20Dockerfile%20CPU-torch%2Buv%20%5BP%5D%22%5D%0A%20%20%20%20T005%5B%22005%20CI%20workflow%20pip-cache%20%5BP%5D%22%5D%0A%0A%20%20%20%20T010%5B%22010%20domain%2Fmodels.py%22%5D%0A%20%20%20%20T011%5B%22011%20domain%2Fports.py%22%5D%0A%0A%20%20%20%20T020%5B%22020%20MinIO%20adapter%20%5BP%5D%22%5D%0A%20%20%20%20T021%5B%22021%20pgvector%20adapter%20%5BP%5D%22%5D%0A%20%20%20%20T022%5B%22022%20embedder%20adapter%20%5BP%5D%22%5D%0A%20%20%20%20T023%5B%22023%20reranker%20adapter%20%5BP%5D%22%5D%0A%20%20%20%20T030%5B%22030%20parsers%20%5BP%5D%22%5D%0A%20%20%20%20T031%5B%22031%20chunker%20%5BP%5D%22%5D%0A%0A%20%20%20%20T040%5B%22040%20IngestPipeline%22%5D%0A%20%20%20%20T041%5B%22041%20HybridRetriever%22%5D%0A%20%20%20%20T042%5B%22042%20RAGInfra%20store.py%22%5D%0A%0A%20%20%20%20T050%5B%22050%20files_router.py%20%5BP%5D%22%5D%0A%20%20%20%20T051%5B%22051%20vector_stores_router.py%20%5BP%5D%22%5D%0A%20%20%20%20T052%5B%22052%20embeddings_router.py%20%5BP%5D%22%5D%0A%20%20%20%20T053%5B%22053%20graph.py%20RAG%20node%20%5BP%5D%22%5D%0A%20%20%20%20T054%5B%22054%20openai_router%20tiktoken%20%5BP%5D%22%5D%0A%20%20%20%20T055%5B%22055%20NATS%20RAG%20handler%20%5BP%5D%22%5D%0A%0A%20%20%20%20T060%5B%22060%20main.py%20wire%22%5D%0A%20%20%20%20T061%5B%22061%20docker-compose.yml%20%5BP%5D%22%5D%0A%20%20%20%20T062%5B%22062%20service.yaml%20bump%20%5BP%5D%22%5D%0A%0A%20%20%20%20T070%5B%22070%20unit%20tests%20rag%2F%20%5BP%5D%22%5D%0A%20%20%20%20T071%5B%22071%20integration%20tests%20routers%20%5BP%5D%22%5D%0A%0A%20%20%20%20T900%5B%22900%20contracts%20openapi%2Basyncapi%20%5BP%5D%22%5D%0A%20%20%20%20T999%5B%22999%20Reviewer%20verification%22%5D%0A%0A%20%20%20%20T001%20--%3E%20T002%0A%20%20%20%20T001%20--%3E%20T010%0A%20%20%20%20T002%20--%3E%20T021%0A%20%20%20%20T010%20--%3E%20T011%0A%20%20%20%20T010%20--%3E%20T020%0A%20%20%20%20T010%20--%3E%20T022%0A%20%20%20%20T010%20--%3E%20T023%0A%20%20%20%20T010%20--%3E%20T030%0A%20%20%20%20T010%20--%3E%20T031%0A%20%20%20%20T011%20--%3E%20T020%0A%20%20%20%20T011%20--%3E%20T021%0A%20%20%20%20T011%20--%3E%20T022%0A%20%20%20%20T011%20--%3E%20T023%0A%20%20%20%20T020%20--%3E%20T040%0A%20%20%20%20T021%20--%3E%20T040%0A%20%20%20%20T021%20--%3E%20T041%0A%20%20%20%20T022%20--%3E%20T040%0A%20%20%20%20T022%20--%3E%20T041%0A%20%20%20%20T023%20--%3E%20T041%0A%20%20%20%20T030%20--%3E%20T040%0A%20%20%20%20T031%20--%3E%20T040%0A%20%20%20%20T040%20--%3E%20T042%0A%20%20%20%20T041%20--%3E%20T042%0A%20%20%20%20T041%20--%3E%20T053%0A%20%20%20%20T042%20--%3E%20T050%0A%20%20%20%20T042%20--%3E%20T051%0A%20%20%20%20T042%20--%3E%20T052%0A%20%20%20%20T042%20--%3E%20T055%0A%20%20%20%20T053%20--%3E%20T054%0A%20%20%20%20T050%20--%3E%20T060%0A%20%20%20%20T051%20--%3E%20T060%0A%20%20%20%20T052%20--%3E%20T060%0A%20%20%20%20T053%20--%3E%20T060%0A%20%20%20%20T054%20--%3E%20T060%0A%20%20%20%20T055%20--%3E%20T060%0A%20%20%20%20T001%20--%3E%20T061%0A%20%20%20%20T001%20--%3E%20T062%0A%20%20%20%20T060%20--%3E%20T070%0A%20%20%20%20T060%20--%3E%20T071%0A%20%20%20%20T060%20--%3E%20T900%0A%20%20%20%20T070%20--%3E%20T999%0A%20%20%20%20T071%20--%3E%20T999%0A%20%20%20%20T900%20--%3E%20T999%0A%20%20%20%20T003%20--%3E%20T999%0A%20%20%20%20T004%20--%3E%20T999%0A%20%20%20%20T005%20--%3E%20T999%0A%20%20%20%20T061%20--%3E%20T999%0A%20%20%20%20T062%20--%3E%20T999%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=c('<hr><h2 id="quality-requirements" tabindex="-1">Quality Requirements <a class="header-anchor" href="#quality-requirements" aria-label="Permalink to &quot;Quality Requirements&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Coverage</th><th>Lint</th></tr></thead><tbody><tr><td><code>rag/</code></td><td>≥ 75%</td><td><code>ruff check src/ &amp;&amp; mypy src/ --strict</code></td></tr><tr><td><code>files_router.py</code></td><td>≥ 75%</td><td>same</td></tr><tr><td><code>vector_stores_router.py</code></td><td>≥ 75%</td><td>same</td></tr><tr><td><code>embeddings_router.py</code></td><td>≥ 75%</td><td>same</td></tr><tr><td>All 012 tests</td><td>pass unchanged</td><td><code>pytest tests/ -k &quot;not rag&quot;</code></td></tr></tbody></table><hr><h2 id="phase-1-setup" tabindex="-1">Phase 1: Setup <a class="header-anchor" href="#phase-1-setup" aria-label="Permalink to &quot;Phase 1: Setup&quot;">​</a></h2><ul><li><p>[x] <strong>[TASK-001]</strong> [REASONER] [P1] Add RAG settings fields to <code>config.py</code></p><ul><li>Dependencies: none</li><li>Module: <code>services/reasoner/src/sherlock/config.py</code></li><li>Acceptance: <code>Settings</code> class has <code>rag_enabled</code>, <code>minio_endpoint</code>, <code>minio_access_key</code> (SecretStr), <code>minio_secret_key</code> (SecretStr), <code>minio_bucket</code>, <code>minio_secure</code>, <code>max_file_bytes</code>, <code>hybrid_alpha</code>, <code>chunk_size_tokens</code>, <code>chunk_overlap_tokens</code>, <code>retrieval_candidate_k</code>, <code>retrieval_top_k</code>, <code>reranker_model</code>, <code>sync_timeout_s</code> with correct env aliases and defaults; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-002]</strong> [PERSISTENCE] [P1] Add RAG schema SQL init script</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/persistence/initdb/004_sherlock_rag_schema.sql</code></li><li>Acceptance: File creates tables <code>sherlock.vector_stores</code>, <code>sherlock.knowledge_files</code>, <code>sherlock.vector_store_files</code>, <code>sherlock.knowledge_chunks</code> with correct columns, FK constraints, HNSW index, GIN FTS index, and <code>fts_vector GENERATED ALWAYS AS</code> column; all <code>IF NOT EXISTS</code> guarded</li></ul></li><li><p>[x] <strong>[TASK-003]</strong> [REASONER] [P1] Add new dependencies to <code>pyproject.toml</code></p><ul><li>Dependencies: none</li><li>Module: <code>services/reasoner/pyproject.toml</code></li><li>Acceptance: <code>minio&gt;=7.2</code>, <code>pypdf&gt;=4.0</code>, <code>python-docx&gt;=1.1</code> added to <code>[project.dependencies]</code>; <code>mypy.overrides</code> for <code>sherlock.rag.adapters.minio</code> with <code>ignore_missing_imports = true</code> added; <code>uv lock</code> runs clean</li></ul></li><li><p>[x] <strong>[TASK-004]</strong> [P] [CI] [P2] Switch Dockerfile to CPU-only torch + uv (M-1 + M-3)</p><ul><li>Dependencies: none</li><li>Module: <code>services/reasoner/Dockerfile</code></li><li>Acceptance: <code>torch</code> installed via <code>--index-url https://download.pytorch.org/whl/cpu</code> before full <code>pip install</code>; or <code>uv sync --frozen --no-dev</code> used; <code>docker build</code> completes; <code>python -c &quot;import torch; print(torch.version.cuda)&quot;</code> returns <code>None</code></li></ul></li><li><p>[x] <strong>[TASK-005]</strong> [P] [CI] [P2] Add pip cache to CI typecheck/test jobs</p><ul><li>Dependencies: none</li><li>Module: <code>.github/workflows/reasoner-images.yml</code></li><li>Acceptance: <code>typecheck</code> and <code>test</code> jobs use <code>actions/setup-python@v5</code> with <code>cache: &#39;pip&#39;</code> and <code>cache-dependency-path: services/reasoner/pyproject.toml</code>; second CI run shows cache hit log</li></ul></li></ul><hr><h2 id="phase-2-foundational" tabindex="-1">Phase 2: Foundational <a class="header-anchor" href="#phase-2-foundational" aria-label="Permalink to &quot;Phase 2: Foundational&quot;">​</a></h2><ul><li><p>[x] <strong>[TASK-010]</strong> [REASONER] [P1] Create <code>rag/domain/models.py</code> with all entity dataclasses</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/reasoner/src/sherlock/rag/domain/models.py</code></li><li>Acceptance: Defines <code>VectorStore</code>, <code>KnowledgeFile</code>, <code>VectorStoreFile</code>, <code>KnowledgeChunk</code>, <code>SearchResult</code>, <code>IngestJob</code>, <code>ParsedDocument</code>; all are Pydantic <code>BaseModel</code> or <code>dataclass</code>; <code>mypy --strict</code> passes; <code>rag/__init__.py</code> and <code>rag/domain/__init__.py</code> exist</li></ul></li><li><p>[x] <strong>[TASK-011]</strong> [REASONER] [P1] Create <code>rag/domain/ports.py</code> with all port Protocols</p><ul><li>Dependencies: TASK-010</li><li>Module: <code>services/reasoner/src/sherlock/rag/domain/ports.py</code></li><li>Acceptance: Defines <code>FileStorePort</code> (upload/download/delete), <code>VectorStorePort</code> (init_schema/upsert_chunks/search_hybrid/delete_by_file/delete_vs), <code>EmbedderPort</code> (encode), <code>RerankerPort</code> (rerank); all use <code>typing.Protocol</code> with <code>@runtime_checkable</code>; <code>mypy --strict</code> passes</li></ul></li></ul><hr><h2 id="phase-3-implementation" tabindex="-1">Phase 3: Implementation <a class="header-anchor" href="#phase-3-implementation" aria-label="Permalink to &quot;Phase 3: Implementation&quot;">​</a></h2><h3 id="parallel-batch-a-—-adapters-run-concurrently-after-task-011" tabindex="-1">Parallel Batch A — Adapters (run concurrently after TASK-011) <a class="header-anchor" href="#parallel-batch-a-—-adapters-run-concurrently-after-task-011" aria-label="Permalink to &quot;Parallel Batch A — Adapters (run concurrently after TASK-011)&quot;">​</a></h3><ul><li><p>[x] <strong>[TASK-020]</strong> [P] [REASONER] [P1] Implement <code>rag/adapters/minio.py</code> (MinIO file store)</p><ul><li>Dependencies: TASK-010, TASK-011</li><li>Module: <code>services/reasoner/src/sherlock/rag/adapters/minio.py</code></li><li>Acceptance: Implements <code>FileStorePort</code>; <code>upload(key, data)</code> creates bucket if absent; <code>download(key)</code> returns bytes; <code>delete(key)</code> removes object; <code>MinioUnavailableError</code> raised (not <code>500</code>) when MinIO unreachable; <code>mypy --strict</code> passes (with mypy override for minio stubs)</li></ul></li><li><p>[x] <strong>[TASK-021]</strong> [P] [REASONER] [P1] Implement <code>rag/adapters/pgvector.py</code> (vector + FTS store)</p><ul><li>Dependencies: TASK-010, TASK-011, TASK-002</li><li>Module: <code>services/reasoner/src/sherlock/rag/adapters/pgvector.py</code></li><li>Acceptance: Implements <code>VectorStorePort</code>; <code>init_schema()</code> is idempotent (CREATE IF NOT EXISTS); <code>upsert_chunks(vs_id, file_id, chunks, embeddings)</code> inserts into <code>knowledge_chunks</code>; <code>search_hybrid(query_vec, query_text, vs_ids, alpha, candidate_k)</code> executes single SQL with <code>alpha</code>-weighted dense + FTS scoring; <code>delete_by_file(file_id)</code> cascades chunks; <code>alpha=0.0</code> uses FTS-only, <code>alpha=1.0</code> uses dense-only; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-022]</strong> [P] [REASONER] [P1] Implement <code>rag/adapters/embedder.py</code> (SentenceTransformer wrapper)</p><ul><li>Dependencies: TASK-010, TASK-011</li><li>Module: <code>services/reasoner/src/sherlock/rag/adapters/embedder.py</code></li><li>Acceptance: Implements <code>EmbedderPort</code>; accepts <code>SentenceTransformer</code> instance (injected, not created); <code>encode(texts) → list[list[float]]</code>; returns 384-dim vectors; handles empty input gracefully; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-023]</strong> [P] [REASONER] [P1] Implement <code>rag/adapters/reranker.py</code> (CrossEncoder wrapper)</p><ul><li>Dependencies: TASK-010, TASK-011</li><li>Module: <code>services/reasoner/src/sherlock/rag/adapters/reranker.py</code></li><li>Acceptance: Implements <code>RerankerPort</code>; lazy <code>CrossEncoder</code> init on first call (model name from settings); <code>_instance</code> cached as class variable after first load — never reloaded; <code>rerank(query, texts) → list[float]</code> wraps <code>CrossEncoder.predict()</code> with <code>asyncio.to_thread(...)</code> since it is a synchronous CPU call; <code>mypy --strict</code> passes</li><li>Note (W-3): First call triggers model download (~200 MB, <code>cross-encoder/ms-marco-MiniLM-L-6-v2</code>). The latency spike on the first RAG chat request is expected and documented. Subsequent calls use the cached model. Pre-downloading at image build time (M-4 in spec CI section) is a later optimisation, not MVP.</li></ul></li></ul><h3 id="parallel-batch-b-—-parsers-chunker-run-concurrently-after-task-010" tabindex="-1">Parallel Batch B — Parsers + Chunker (run concurrently after TASK-010) <a class="header-anchor" href="#parallel-batch-b-—-parsers-chunker-run-concurrently-after-task-010" aria-label="Permalink to &quot;Parallel Batch B — Parsers + Chunker (run concurrently after TASK-010)&quot;">​</a></h3><ul><li><p>[x] <strong>[TASK-030]</strong> [P] [REASONER] [P1] Implement <code>rag/parsers/</code> (all format parsers)</p><ul><li>Dependencies: TASK-010</li><li>Module: <code>services/reasoner/src/sherlock/rag/parsers/</code></li><li>Acceptance: <code>base.py</code> defines <code>ParsedDocument(text: str, metadata: dict)</code> and <code>ParserPort</code> Protocol; <code>text_parser.py</code> handles <code>.txt,.md,.rst,.py,.go,.ts,.js,.tsx,.jsx</code> (UTF-8); <code>pdf_parser.py</code> uses <code>pypdf</code>; <code>docx_parser.py</code> uses <code>python-docx</code>; <code>json_parser.py</code> pretty-prints; <code>csv_parser.py</code> yields one chunk per row; <code>__init__.py</code> exports <code>dispatch_parser(filename, data) → ParsedDocument</code>; unsupported extension raises <code>UnsupportedFileTypeError</code>; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-031]</strong> [P] [REASONER] [P1] Implement <code>rag/chunker.py</code> (token-aware text chunker)</p><ul><li>Dependencies: TASK-010</li><li>Module: <code>services/reasoner/src/sherlock/rag/chunker.py</code></li><li>Acceptance: <code>chunk_text(text, chunk_size, overlap) → list[str]</code>; uses <code>tiktoken</code> (<code>cl100k_base</code>) for token counting; respects <code>chunk_size_tokens</code> and <code>chunk_overlap_tokens</code> from settings; empty input → empty list; <code>mypy --strict</code> passes</li></ul></li></ul><h3 id="application-layer-sequential-within-stream" tabindex="-1">Application Layer (sequential within stream) <a class="header-anchor" href="#application-layer-sequential-within-stream" aria-label="Permalink to &quot;Application Layer (sequential within stream)&quot;">​</a></h3><ul><li><p>[x] <strong>[TASK-040]</strong> [REASONER] [P1] Implement <code>rag/application/ingest.py</code> (IngestPipeline)</p><ul><li>Dependencies: TASK-020, TASK-021, TASK-022, TASK-030, TASK-031</li><li>Module: <code>services/reasoner/src/sherlock/rag/application/ingest.py</code></li><li>Acceptance: <code>IngestPipeline.ingest(file_id, vs_id) → int</code> downloads file from MinIO, dispatches parser, chunks, embeds, batch-inserts <code>knowledge_chunks</code>, updates <code>vector_store_files.status</code> to <code>completed</code>/<code>failed</code>; on exception sets <code>status=failed</code> and <code>error_message</code> in DB; returns <code>chunk_count</code>; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-041]</strong> [REASONER] [P1] Implement <code>rag/application/retrieve.py</code> (HybridRetriever)</p><ul><li>Dependencies: TASK-021, TASK-022, TASK-023</li><li>Module: <code>services/reasoner/src/sherlock/rag/application/retrieve.py</code></li><li>Acceptance: <code>HybridRetriever.search(query, vs_ids, alpha, candidate_k, top_k) → list[SearchResult]</code>; encodes query, calls <code>search_hybrid</code>, then reranks to <code>top_k</code>; non-existent <code>vs_id</code> → empty results (no error); <code>alpha</code> per-request overrides default; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-042]</strong> [REASONER] [P1] Implement <code>rag/store.py</code> (RAGInfra factory + dataclass)</p><ul><li>Dependencies: TASK-040, TASK-041</li><li>Module: <code>services/reasoner/src/sherlock/rag/store.py</code></li><li>Acceptance: <code>RAGInfra</code> dataclass holds <code>file_store</code>, <code>vector_store</code>, <code>embedder</code>, <code>reranker</code>, <code>ingest_pipeline</code>, <code>retriever</code>, <code>settings</code>; <code>async build_rag_infra(settings, encoder) → RAGInfra</code> wires all adapters and application objects; calls <code>pgvector_adapter.init_schema()</code> and MinIO bucket creation on startup; <code>mypy --strict</code> passes</li></ul></li></ul><h3 id="parallel-batch-c-—-http-routers-run-concurrently-after-task-042" tabindex="-1">Parallel Batch C — HTTP Routers (run concurrently after TASK-042) <a class="header-anchor" href="#parallel-batch-c-—-http-routers-run-concurrently-after-task-042" aria-label="Permalink to &quot;Parallel Batch C — HTTP Routers (run concurrently after TASK-042)&quot;">​</a></h3><ul><li><p>[x] <strong>[TASK-050]</strong> [P] [REASONER] [P1] Implement <code>files_router.py</code> (US-1, US-6, FR-1/2/3)</p><ul><li>Dependencies: TASK-042</li><li>Module: <code>services/reasoner/src/sherlock/files_router.py</code></li><li>Acceptance: <code>POST /v1/files</code> → 201 (multipart); validates extension + size (<code>UnsupportedFileTypeError</code> → 400, size &gt; <code>max_file_bytes</code> → 400); uploads to MinIO, inserts <code>knowledge_files</code>; <code>GET /v1/files</code> → 200 list; <code>GET /v1/files/{id}</code> → 200 or 404; <code>DELETE /v1/files/{id}</code> → 200 (cascades chunks + MinIO delete); <code>GET /v1/files/{id}/content</code> → streamed bytes; all routes return 503 when <code>state.rag is None</code>; OTEL counter <code>sherlock.rag.uploads.total</code>; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-051]</strong> [P] [REASONER] [P1] Implement <code>vector_stores_router.py</code> (US-2/3/8/9/10, FR-4/5/6/7)</p><ul><li>Dependencies: TASK-042</li><li>Module: <code>services/reasoner/src/sherlock/vector_stores_router.py</code></li><li>Acceptance: <code>POST /v1/vector_stores</code> → 201; <code>GET/DELETE /v1/vector_stores/{id}</code> → 200/404; <code>POST /v1/vector_stores/{id}/files</code> → 202 async (<code>BackgroundTask</code>) or 200 sync (<code>?sync=true</code>); <code>?sync=true</code> uses <code>asyncio.wait_for(settings.sync_timeout_s)</code>, falls back to 202 on timeout; <code>GET /v1/vector_stores/{id}/files/{fid}</code> → shows status/chunk_count/error; <code>DELETE /v1/vector_stores/{id}/files/{fid}</code> → removes chunks; <code>POST /v1/vector_stores/{id}/search</code> → hybrid search with optional <code>hybrid_alpha</code> override; same-file-twice → idempotent (returns existing row); all routes 503 when <code>state.rag is None</code>; OTEL timers <code>sherlock.rag.ingest.latency</code>, <code>sherlock.rag.search.latency</code>; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-052]</strong> [P] [REASONER] [P1] Implement <code>embeddings_router.py</code> (US-5, FR-8)</p><ul><li>Dependencies: TASK-042</li><li>Module: <code>services/reasoner/src/sherlock/embeddings_router.py</code></li><li>Acceptance: <code>POST /v1/embeddings</code> → 200 <code>{ &quot;object&quot;: &quot;list&quot;, &quot;data&quot;: [{ &quot;embedding&quot;: [...384 floats...], &quot;index&quot;: 0, &quot;object&quot;: &quot;embedding&quot; }] }</code>; uses shared <code>EmbedderAdapter</code>; supports batch input (<code>input</code> as string or list); <code>usage.prompt_tokens</code> populated via tiktoken; 503 when <code>state.rag is None</code>; OTEL timer <code>sherlock.rag.embed.latency</code>; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-053]</strong> [P] [REASONER] [P1] Extend <code>graph.py</code> with RAG retrieve node (FR-9)</p><ul><li>Dependencies: TASK-041</li><li>Module: <code>services/reasoner/src/sherlock/graph.py</code></li><li>Acceptance: New <code>rag_retrieve</code> node runs when <code>vector_store_ids</code> is non-empty; calls <code>HybridRetriever.search</code>; injects knowledge context into LLM prompt after system prompt and conv memory; when <code>vector_store_ids</code> is absent/empty node is a no-op (all existing 012 tests pass unchanged); <code>invoke_graph</code> signature extended with optional <code>vector_store_ids: list[str] | None = None</code>; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-054]</strong> [P] [REASONER] [P1] Wire tiktoken token counts + file_search tool detection in <code>openai_router.py</code> (FR-9, FR-14, US-13)</p><ul><li>Dependencies: TASK-053</li><li>Module: <code>services/reasoner/src/sherlock/openai_router.py</code></li><li>Acceptance: <code>_count_tokens(model, text) → int</code> helper using <code>tiktoken.encoding_for_model</code> with <code>cl100k_base</code> fallback; <code>usage.prompt_tokens</code> and <code>completion_tokens</code> are non-zero in all <code>/v1/chat/completions</code> responses; when <code>req.tools</code> contains <code>{ &quot;type&quot;: &quot;file_search&quot;, &quot;vector_store_ids&quot;: [...] }</code> → extracts <code>vector_store_ids</code> and passes to <code>invoke_graph</code>; when <code>req.tools</code> contains <code>hybrid_alpha</code> → passes to graph; all existing streaming + non-streaming code paths pass; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-055]</strong> [P] [REASONER] [P2] Implement <code>rag/nats_handler.py</code> (RAG NATS subjects, US-12)</p><ul><li>Dependencies: TASK-042</li><li>Module: <code>services/reasoner/src/sherlock/rag/nats_handler.py</code></li><li>Acceptance: Subscribes to <code>sherlock.v1.rag.ingest.request</code> → runs <code>IngestPipeline.ingest</code> → publishes to <code>sherlock.v1.rag.ingest.result</code>; subscribes to <code>sherlock.v1.rag.search.request</code> → runs <code>HybridRetriever.search</code> → publishes result; subscribes to <code>sherlock.v1.rag.embed.request</code> → runs <code>EmbedderAdapter.encode</code> → publishes result; subscribes to <code>sherlock.v1.rag.chat.request</code> → runs graph with <code>vector_store_ids</code> → publishes result; <code>mypy --strict</code> passes</li></ul></li></ul><hr><h2 id="phase-4-integration" tabindex="-1">Phase 4: Integration <a class="header-anchor" href="#phase-4-integration" aria-label="Permalink to &quot;Phase 4: Integration&quot;">​</a></h2><ul><li><p>[x] <strong>[TASK-060]</strong> [REASONER] [P1] Wire <code>RAGInfra</code> into <code>main.py</code> (AppState + lifespan + router mounts)</p><ul><li>Dependencies: TASK-042, TASK-050, TASK-051, TASK-052, TASK-053, TASK-054, TASK-055</li><li>Module: <code>services/reasoner/src/sherlock/main.py</code></li><li>Acceptance: <code>AppState</code> has <code>rag: RAGInfra | None = None</code>; lifespan calls <code>build_rag_infra(settings, memory._encoder)</code> only when <code>settings.rag_enabled</code>; <code>files_router</code>, <code>vector_stores_router</code>, <code>embeddings_router</code> included with prefix <code>/v1</code>; NATS RAG handler started when <code>rag_enabled and nats_enabled</code>; <code>/health/deep</code> adds <code>&quot;minio&quot;: await state.rag.file_store.health_check()</code> to the <code>components</code> dict when <code>state.rag is not None</code> — MinIO unhealthy → overall status 503 (W-2); startup log shows <code>rag_enabled=true/false</code>; <code>mypy --strict</code> passes</li></ul></li><li><p>[x] <strong>[TASK-061]</strong> [P] [REASONER] [P1] Update <code>docker-compose.yml</code> with RAG env vars</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/reasoner/docker-compose.yml</code></li><li>Acceptance: Add to <code>arc-sherlock</code> environment block: <code>SHERLOCK_RAG_ENABLED: &quot;false&quot;</code> (default off), <code>SHERLOCK_MINIO_ENDPOINT: &quot;arc-storage:9000&quot;</code>, <code>SHERLOCK_MINIO_ACCESS_KEY: &quot;arc&quot;</code>, <code>SHERLOCK_MINIO_SECRET_KEY: &quot;arc-minio-dev&quot;</code> (matches <code>services/storage/docker-compose.yml</code> <code>MINIO_ROOT_*</code>), <code>SHERLOCK_MINIO_BUCKET: &quot;sherlock-files&quot;</code>; no other structural changes needed — <code>arc-storage</code> service wiring is handled by <code>services/profiles.yaml</code> (<code>reason</code> profile already includes <code>storage</code>) and <code>service.yaml</code> startup ordering is handled by <code>resolve-deps.sh</code> (W-1, W-4)</li></ul></li><li><p>[x] <strong>[TASK-062]</strong> [P] [REASONER] [P1] Bump <code>service.yaml</code> to v0.3.0, add <code>storage</code> depends_on</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/reasoner/service.yaml</code></li><li>Acceptance: <code>version: &quot;0.3.0&quot;</code>; <code>depends_on</code> list includes <code>storage</code>; <code>description</code> updated to mention RAG</li></ul></li><li><p>[x] <strong>[TASK-070]</strong> [P] [REASONER] [P1] Write unit tests for <code>rag/</code> package</p><ul><li>Dependencies: TASK-060</li><li>Module: <code>services/reasoner/tests/rag/</code></li><li>Acceptance: Tests cover <code>domain/models.py</code>, <code>adapters/embedder.py</code>, <code>adapters/reranker.py</code>, <code>parsers/</code> (all formats + dispatch), <code>chunker.py</code>, <code>application/ingest.py</code> (mocked adapters), <code>application/retrieve.py</code> (mocked adapters); <code>hybrid_alpha=0.0</code> and <code>alpha=1.0</code> SQL edge cases tested; <code>IngestPipeline</code> exception → <code>status=failed</code>; <code>pytest tests/ -k &quot;rag&quot; --cov=sherlock.rag</code> ≥ 75%</li></ul></li><li><p>[x] <strong>[TASK-071]</strong> [P] [REASONER] [P1] Write integration tests for all 3 new routers + RAG chat</p><ul><li>Dependencies: TASK-060</li><li>Module: <code>services/reasoner/tests/</code></li><li>Acceptance: <code>httpx.AsyncClient</code> tests for <code>POST /v1/files</code>, <code>GET /v1/files</code>, <code>DELETE /v1/files/{id}</code>, <code>POST /v1/vector_stores</code>, <code>POST /v1/vector_stores/{id}/files</code>, <code>GET /v1/vector_stores/{id}/files/{fid}</code>, <code>POST /v1/vector_stores/{id}/search</code>, <code>POST /v1/embeddings</code>; <code>SHERLOCK_RAG_ENABLED=false</code> → all new routes return 503; <code>usage.prompt_tokens</code> non-zero in chat response; all 012 tests still pass (<code>pytest tests/ -k &quot;not rag&quot;</code>); overall rag router coverage ≥ 75%</li></ul></li></ul><hr><h2 id="phase-5-polish" tabindex="-1">Phase 5: Polish <a class="header-anchor" href="#phase-5-polish" aria-label="Permalink to &quot;Phase 5: Polish&quot;">​</a></h2><ul><li><p>[x] <strong>[TASK-900]</strong> [P] [DOCS] [P1] Update OpenAPI and AsyncAPI contracts</p><ul><li>Dependencies: TASK-060</li><li>Module: <code>specs/013-sherlock-rag-api/contracts/openapi.yaml</code>, <code>specs/013-sherlock-rag-api/contracts/asyncapi.yaml</code></li><li>Acceptance: <code>openapi.yaml</code> includes all 13 new paths (<code>/v1/files</code>, <code>/v1/files/{id}</code>, <code>/v1/files/{id}/content</code>, <code>/v1/vector_stores</code>, <code>/v1/vector_stores/{id}</code>, <code>/v1/vector_stores/{id}/files</code>, <code>/v1/vector_stores/{id}/files/{fid}</code>, <code>/v1/vector_stores/{id}/search</code>, <code>/v1/embeddings</code>) with request/response schemas; <code>asyncapi.yaml</code> includes 4 channel pairs (<code>sherlock.v1.rag.ingest.*</code>, <code>sherlock.v1.rag.chat.*</code>, <code>sherlock.v1.rag.search.*</code>, <code>sherlock.v1.rag.embed.*</code>); files pass YAML lint</li></ul></li><li><p>[x] <strong>[TASK-999]</strong> [REVIEW] [P1] Reviewer agent verification</p><ul><li>Dependencies: ALL tasks</li><li>Module: all affected modules</li><li>Acceptance: All tasks above are <code>[x]</code> done; <code>ruff check src/ &amp;&amp; mypy src/ --strict</code> zero errors; <code>pytest tests/ --cov=sherlock</code> ≥ 75% on rag/ + new routers; <code>pytest tests/ -k &quot;not rag&quot;</code> all pass; <code>usage.prompt_tokens</code> non-zero; NATS RAG subjects verified; <code>service.yaml</code> version = <code>0.3.0</code>; <code>openapi.yaml</code> and <code>asyncapi.yaml</code> updated; no file content in logs/spans; <code>SecretStr</code> on MinIO creds; <code>SHERLOCK_RAG_ENABLED=false</code> → 012 identical; CI Dockerfile produces CPU-only torch</li></ul></li></ul><hr><h2 id="progress-summary" tabindex="-1">Progress Summary <a class="header-anchor" href="#progress-summary" aria-label="Permalink to &quot;Progress Summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Phase</th><th>Total</th><th>Done</th><th>Parallel</th></tr></thead><tbody><tr><td>Setup</td><td>5</td><td>0</td><td>2</td></tr><tr><td>Foundational</td><td>2</td><td>0</td><td>0</td></tr><tr><td>Implementation</td><td>15</td><td>0</td><td>12</td></tr><tr><td>Integration</td><td>6</td><td>0</td><td>4</td></tr><tr><td>Polish</td><td>2</td><td>1</td><td>1</td></tr><tr><td><strong>Total</strong></td><td><strong>30</strong></td><td><strong>1</strong></td><td><strong>19</strong></td></tr></tbody></table>',28))])}const v=s(u,[["render",h]]);export{k as __pageData,v as default};
