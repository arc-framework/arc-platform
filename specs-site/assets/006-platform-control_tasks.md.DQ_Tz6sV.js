import{_ as i,C as a,o,c as t,a2 as c,b as s,w as l,a as r,E as n,a3 as u}from"./chunks/framework.C-cuuT8Z.js";const b=JSON.parse('{"title":"Tasks: Platform Control Plane Services Setup","description":"","frontmatter":{},"headers":[],"relativePath":"006-platform-control/tasks.md","filePath":"006-platform-control/tasks.md","lastUpdated":1772310733000}'),p={name:"006-platform-control/tasks.md"};function h(m,e,f,k,y,g){const d=a("Mermaid");return o(),t("div",null,[e[1]||(e[1]=c(`<h1 id="tasks-platform-control-plane-services-setup" tabindex="-1">Tasks: Platform Control Plane Services Setup <a class="header-anchor" href="#tasks-platform-control-plane-services-setup" aria-label="Permalink to &quot;Tasks: Platform Control Plane Services Setup&quot;">​</a></h1><blockquote><p><strong>Spec</strong>: 006-platform-control <strong>Date</strong>: 2026-02-28</p></blockquote><h2 id="task-format" tabindex="-1">Task Format <a class="header-anchor" href="#task-format" aria-label="Permalink to &quot;Task Format&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[TASK-NNN] [P?] [MODULE] [PRIORITY] Description</span></span>
<span class="line"><span>  Dependencies: [TASK-XXX] or none</span></span>
<span class="line"><span>  Module: services/{role}</span></span>
<span class="line"><span>  Acceptance: Testable criteria</span></span>
<span class="line"><span>  Status: [ ] pending | [~] in-progress | [x] done</span></span></code></pre></div><ul><li><code>[P]</code> = Safe for parallel agent execution</li><li>Priority: P1 (must), P2 (should), P3 (nice)</li></ul><h2 id="dependency-graph" tabindex="-1">Dependency Graph <a class="header-anchor" href="#dependency-graph" aria-label="Permalink to &quot;Dependency Graph&quot;">​</a></h2>`,6)),(o(),s(u,null,{default:l(()=>[n(d,{id:"mermaid-27",class:"mermaid",graph:"graph%20TD%0A%20%20%20%20T001%5B%22TASK-001%5Cnprofiles.yaml%22%5D%20--%3E%20T011%0A%20%20%20%20T001%20--%3E%20T012%0A%20%20%20%20T001%20--%3E%20T013%0A%0A%20%20%20%20T011%5B%22TASK-011%20%5BP%5D%5Cnservices%2Fgateway%2F%5CnHeimdall%22%5D%20--%3E%20T021%0A%20%20%20%20T012%5B%22TASK-012%20%5BP%5D%5Cnservices%2Fsecrets%2F%5CnNick%20Fury%22%5D%20--%3E%20T022%0A%20%20%20%20T013%5B%22TASK-013%20%5BP%5D%5Cnservices%2Fflags%2F%5CnMystique%22%5D%20--%3E%20T023%0A%0A%20%20%20%20T021%5B%22TASK-021%20%5BP%5D%5Cnheimdall.mk%22%5D%20--%3E%20T024%0A%20%20%20%20T022%5B%22TASK-022%20%5BP%5D%5Cnnick-fury.mk%22%5D%20--%3E%20T024%0A%20%20%20%20T023%5B%22TASK-023%20%5BP%5D%5Cnmystique.mk%22%5D%20--%3E%20T024%0A%0A%20%20%20%20T024%5B%22TASK-024%5Cncontrol.mk%20%2B%5CnMakefile%20includes%22%5D%20--%3E%20T031%0A%20%20%20%20T024%20--%3E%20T032%0A%0A%20%20%20%20T031%5B%22TASK-031%20%5BP%5D%5Cncontrol-images.yml%22%5D%20--%3E%20T041%0A%20%20%20%20T032%5B%22TASK-032%20%5BP%5D%5Cncontrol-release.yml%22%5D%20--%3E%20T041%0A%0A%20%20%20%20T041%5B%22TASK-041%5CnE2E%20verification%22%5D%20--%3E%20T900%0A%20%20%20%20T041%20--%3E%20T999%0A%0A%20%20%20%20T900%5B%22TASK-900%5CnDocs%20%26%20links%22%5D%20--%3E%20T999%0A%20%20%20%20T999%5B%22TASK-999%5CnReviewer%22%5D%0A"})]),fallback:l(()=>[...e[0]||(e[0]=[r(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=c(`<h2 id="quality-requirements" tabindex="-1">Quality Requirements <a class="header-anchor" href="#quality-requirements" aria-label="Permalink to &quot;Quality Requirements&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Coverage</th><th>Lint</th><th>Notes</th></tr></thead><tbody><tr><td>Config/YAML</td><td>n/a</td><td><code>docker compose config</code> (no syntax errors)</td><td></td></tr><tr><td>Makefile</td><td>n/a</td><td><code>make -n {target}</code> dry-run passes</td><td></td></tr><tr><td>CI/CD YAML</td><td>n/a</td><td>GitHub Actions YAML schema valid</td><td></td></tr></tbody></table><hr><h2 id="phase-1-setup" tabindex="-1">Phase 1: Setup <a class="header-anchor" href="#phase-1-setup" aria-label="Permalink to &quot;Phase 1: Setup&quot;">​</a></h2><ul><li>[x] [TASK-001] [SERVICES] [P1] Update profiles.yaml — heimdall → <code>think</code>; nick-fury + mystique → <code>reason</code><ul><li>Dependencies: none</li><li>Module: <code>services/profiles.yaml</code></li><li>Acceptance: <ul><li>Python YAML validation confirms structure (not just text presence):<div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>python3 -c &quot;</span></span>
<span class="line"><span>import yaml</span></span>
<span class="line"><span>p = yaml.safe_load(open(&#39;services/profiles.yaml&#39;))</span></span>
<span class="line"><span>assert &#39;heimdall&#39; in p[&#39;think&#39;][&#39;services&#39;], &#39;heimdall missing from think&#39;</span></span>
<span class="line"><span>assert &#39;nick-fury&#39; in p[&#39;reason&#39;][&#39;services&#39;], &#39;nick-fury missing from reason&#39;</span></span>
<span class="line"><span>assert &#39;mystique&#39; in p[&#39;reason&#39;][&#39;services&#39;], &#39;mystique missing from reason&#39;</span></span>
<span class="line"><span>assert p[&#39;ultra-instinct&#39;][&#39;services&#39;] == &#39;*&#39;, &#39;ultra-instinct changed&#39;</span></span>
<span class="line"><span>print(&#39;profiles.yaml OK&#39;)</span></span>
<span class="line"><span>&quot;</span></span></code></pre></div></li><li>Existing services in <code>think</code> preserved: flash, sonic, strange, friday-collector, cortex, oracle, cerebro</li><li>Existing services in <code>reason</code> preserved: cortex, flash, strange, sonic, otel, tardis</li></ul></li></ul></li></ul><hr><h2 id="phase-2-foundational-—-service-directories-parallel" tabindex="-1">Phase 2: Foundational — Service Directories (parallel) <a class="header-anchor" href="#phase-2-foundational-—-service-directories-parallel" aria-label="Permalink to &quot;Phase 2: Foundational — Service Directories (parallel)&quot;">​</a></h2><p>All three tasks are fully independent. Each creates: <code>Dockerfile</code>, <code>service.yaml</code>, <code>docker-compose.yml</code>. Make targets handled in Phase 3.</p><ul><li><p>[x] [TASK-011] [P] [SERVICES] [P1] Create <code>services/gateway/</code> — Heimdall (Traefik v3)</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/gateway/</code></li><li>Acceptance: <ul><li><code>Dockerfile</code>: <code>FROM traefik:v3</code>; OCI labels (<code>org.opencontainers.image.title</code>, <code>description</code>, <code>source</code>); <code>arc.service.name=arc-gateway</code>, <code>arc.service.codename=heimdall</code>, <code>arc.service.tech=traefik</code>; <code>USER 1000</code> (non-root via non-privileged internal ports)</li><li><code>service.yaml</code>: name <code>arc-gateway</code>, codename <code>heimdall</code>, image <code>ghcr.io/arc-framework/arc-gateway:latest</code>, tech <code>traefik</code>, upstream <code>traefik:v3</code>, ports <code>[80, 8090]</code>, health <code>http://localhost:8090/ping</code>, depends_on <code>[]</code></li><li><code>docker-compose.yml</code>: service <code>arc-gateway</code>; command includes <code>--api.insecure=true</code>, <code>--api.dashboard=true</code>, <code>--ping=true</code>, <code>--providers.docker=true</code>, <code>--providers.docker.network=arc_platform_net</code>, <code>--providers.docker.exposedbydefault=false</code>, <code>--entrypoints.web.address=:8080</code>, <code>--entrypoints.dashboard.address=:8090</code>; ports <code>127.0.0.1:80:8080</code> and <code>127.0.0.1:8090:8090</code>; volumes <code>/var/run/docker.sock:/var/run/docker.sock:ro</code>; <code>user: &quot;1000:1000&quot;</code>; healthcheck <code>traefik healthcheck --ping</code> interval 5s, timeout 3s, retries 5, start_period 5s; no persistent volume; network <code>arc_platform_net</code> (external); <code>restart: unless-stopped</code></li><li><code>docker compose config</code> passes without errors</li><li><code>docker build -f services/gateway/Dockerfile services/gateway/</code> succeeds</li><li>Note: if <code>USER 1000</code> causes Traefik to fail at runtime, remove <code>USER 1000</code> and document root deviation in Dockerfile comment (same Pulsar pattern from 003)</li></ul></li></ul></li><li><p>[x] [TASK-012] [P] [SERVICES] [P1] Create <code>services/secrets/</code> — Nick Fury (OpenBao)</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/secrets/</code></li><li>Acceptance: <ul><li><code>Dockerfile</code>: <code>FROM openbao/openbao</code>; OCI labels; <code>arc.service.name=arc-vault</code>, <code>arc.service.codename=nick-fury</code>, <code>arc.service.tech=openbao</code>; <strong>Dockerfile MUST contain</strong> this exact comment (or equivalent): <code># openbao/openbao runs as root by default. Nick Fury uses -dev mode: in-memory, auto-unsealed, known root token. Root is acceptable for this DEVELOPMENT-ONLY service. For production: Raft-backed storage + TLS + non-root user.</code></li><li><code>service.yaml</code>: name <code>arc-vault</code>, codename <code>nick-fury</code>, image <code>ghcr.io/arc-framework/arc-vault:latest</code>, tech <code>openbao</code>, upstream <code>openbao/openbao</code>, ports <code>[8200]</code>, health <code>http://localhost:8200/v1/sys/health</code>, depends_on <code>[]</code></li><li><code>docker-compose.yml</code>: service <code>arc-vault</code>; command <code>server -dev</code>; env <code>VAULT_DEV_ROOT_TOKEN_ID=arc-dev-token</code>, <code>VAULT_DEV_LISTEN_ADDRESS=0.0.0.0:8200</code>; port <code>127.0.0.1:8200:8200</code>; no volume (stateless dev); healthcheck: attempt <code>wget -qO- http://localhost:8200/v1/sys/health || exit 1</code>; if <code>wget</code> absent use bash <code>/dev/tcp</code> pattern (same as arc-vector-db); interval 5s, timeout 3s, retries 5, start_period 5s; network <code>arc_platform_net</code> (external); <code>restart: unless-stopped</code></li><li><code>docker compose config</code> passes without errors</li><li><code>docker build -f services/secrets/Dockerfile services/secrets/</code> succeeds</li><li>No volume declared (Nick Fury is stateless in dev — data lost on restart is expected and documented)</li></ul></li></ul></li><li><p>[x] [TASK-013] [P] [SERVICES] [P1] Create <code>services/flags/</code> — Mystique (Unleash)</p><ul><li>Dependencies: TASK-001</li><li>Module: <code>services/flags/</code>, <code>services/persistence/initdb/</code></li><li>Acceptance: <ul><li><code>Dockerfile</code>: <code>FROM unleashorg/unleash-server</code>; OCI labels; <code>arc.service.name=arc-flags</code>, <code>arc.service.codename=mystique</code>, <code>arc.service.tech=unleash</code>; attempt <code>USER 1000</code> (non-root); if Unleash fails to start, remove and add comment: &quot;# NOTE: unleashorg/unleash-server requires root — upstream constraint&quot;</li><li><code>service.yaml</code>: name <code>arc-flags</code>, codename <code>mystique</code>, image <code>ghcr.io/arc-framework/arc-flags:latest</code>, tech <code>unleash</code>, upstream <code>unleashorg/unleash-server</code>, ports <code>[4242]</code>, health <code>http://localhost:4242/health</code>, depends_on <code>[oracle, sonic]</code></li><li><code>docker-compose.yml</code>: service <code>arc-flags</code>; env <code>DATABASE_URL=postgresql://arc:arc@arc-sql-db:5432/unleash</code>, <code>REDIS_HOST=arc-cache</code>, <code>REDIS_PORT=6379</code>; port <code>127.0.0.1:4242:4242</code>; no persistent volume; healthcheck <code>wget -qO- http://localhost:4242/health || exit 1</code> interval 10s, timeout 5s, retries 10, <strong>start_period 30s</strong> (Unleash runs DB migrations on first boot); network <code>arc_platform_net</code> (external); <code>restart: unless-stopped</code></li><li><strong>Oracle init SQL</strong> (cross-service, justified): create <code>services/persistence/initdb/002_create_unleash_db.sql</code> containing <code>CREATE DATABASE unleash;</code><ul><li>Justification: Oracle (the database service) is the rightful owner of &quot;what databases exist&quot;. Mystique declares the need; Oracle satisfies it via initdb. This follows the same pattern as <code>001_schema_migrations.sql</code> (created in 005 for Cortex&#39;s needs). Not a Principle III violation — initdb is Oracle&#39;s extension point for dependent services.</li><li>This file runs on Oracle&#39;s first boot only (Postgres skips initdb if data dir exists). To force re-creation: <code>make oracle-nuke &amp;&amp; make oracle-up</code></li></ul></li><li><code>docker compose config</code> passes without errors</li><li><code>docker build -f services/flags/Dockerfile services/flags/</code> succeeds</li></ul></li></ul></li></ul><hr><h2 id="phase-3-make-targets" tabindex="-1">Phase 3: Make Targets <a class="header-anchor" href="#phase-3-make-targets" aria-label="Permalink to &quot;Phase 3: Make Targets&quot;">​</a></h2><h3 id="batch-a-—-parallel-each-mk-is-independent" tabindex="-1">Batch A — parallel (each .mk is independent) <a class="header-anchor" href="#batch-a-—-parallel-each-mk-is-independent" aria-label="Permalink to &quot;Batch A — parallel (each .mk is independent)&quot;">​</a></h3><ul><li><p>[x] [TASK-021] [P] [SERVICES] [P1] Create <code>services/gateway/heimdall.mk</code></p><ul><li>Dependencies: TASK-011</li><li>Module: <code>services/gateway/heimdall.mk</code></li><li>Acceptance: <ul><li>Targets present: <code>heimdall-help</code>, <code>heimdall-build</code>, <code>heimdall-build-fresh</code>, <code>heimdall-up</code>, <code>heimdall-down</code>, <code>heimdall-health</code>, <code>heimdall-logs</code>, <code>heimdall-push</code>, <code>heimdall-publish</code>, <code>heimdall-tag</code>, <code>heimdall-clean</code>, <code>heimdall-nuke</code></li><li><code>heimdall-health</code>: probes <code>curl -sf http://localhost:8090/ping</code>; exits 0 if healthy, 1 if not</li><li><code>heimdall-clean</code> / <code>heimdall-nuke</code>: require typed confirmation (<code>yes</code> / <code>nuke</code>) before destructive action</li><li><code>heimdall-publish</code>: pushes image then prints <code>https://github.com/orgs/$(ORG)/packages/container/arc-gateway/settings</code></li><li><code>make heimdall-help</code> lists all targets with descriptions; output includes note: &quot;Port 80 binding requires Docker privilege. If startup fails with &#39;permission denied&#39;, change host port in a docker-compose.override.yml&quot;</li><li>All targets use <code>COLOR_INFO</code>, <code>COLOR_OK</code>, <code>COLOR_ERR</code> from root Makefile</li><li><code>make -n heimdall-up</code> dry-run passes</li></ul></li></ul></li><li><p>[x] [TASK-022] [P] [SERVICES] [P1] Create <code>services/secrets/nick-fury.mk</code></p><ul><li>Dependencies: TASK-012</li><li>Module: <code>services/secrets/nick-fury.mk</code></li><li>Acceptance: <ul><li>Targets present: <code>nick-fury-help</code>, <code>nick-fury-build</code>, <code>nick-fury-build-fresh</code>, <code>nick-fury-up</code>, <code>nick-fury-down</code>, <code>nick-fury-health</code>, <code>nick-fury-logs</code>, <code>nick-fury-push</code>, <code>nick-fury-publish</code>, <code>nick-fury-tag</code>, <code>nick-fury-clean</code>, <code>nick-fury-nuke</code></li><li><code>nick-fury-health</code>: probes <code>curl -sf http://localhost:8200/v1/sys/health</code>; exits 0 if healthy, 1 if not</li><li><code>nick-fury-nuke</code>: warns &quot;All secrets will be lost (stateless dev service)&quot; before destructive action; requires typed confirmation</li><li><code>nick-fury-help</code> output mentions &quot;Dev mode only — all secrets lost on restart&quot;</li><li><code>make nick-fury-help</code> lists all targets</li><li><code>make -n nick-fury-up</code> dry-run passes</li></ul></li></ul></li><li><p>[x] [TASK-023] [P] [SERVICES] [P1] Create <code>services/flags/mystique.mk</code></p><ul><li>Dependencies: TASK-013</li><li>Module: <code>services/flags/mystique.mk</code></li><li>Acceptance: <ul><li>Targets present: <code>mystique-help</code>, <code>mystique-build</code>, <code>mystique-build-fresh</code>, <code>mystique-up</code>, <code>mystique-down</code>, <code>mystique-health</code>, <code>mystique-logs</code>, <code>mystique-push</code>, <code>mystique-publish</code>, <code>mystique-tag</code>, <code>mystique-clean</code>, <code>mystique-nuke</code></li><li><code>mystique-health</code>: probes <code>curl -sf http://localhost:4242/health</code>; exits 0 if healthy, 1 if not</li><li><code>mystique-help</code> output includes note: &quot;Requires arc-sql-db and arc-cache to be running&quot;</li><li><code>mystique-clean</code> / <code>mystique-nuke</code>: require typed confirmation before destructive action</li><li><code>make mystique-help</code> lists all targets</li><li><code>make -n mystique-up</code> dry-run passes</li></ul></li></ul></li></ul><h3 id="batch-b-—-sequential-depends-on-all-batch-a" tabindex="-1">Batch B — sequential (depends on all Batch A) <a class="header-anchor" href="#batch-b-—-sequential-depends-on-all-batch-a" aria-label="Permalink to &quot;Batch B — sequential (depends on all Batch A)&quot;">​</a></h3><ul><li>[x] [TASK-024] [SERVICES] [P1] Create <code>services/control.mk</code> + update root <code>Makefile</code><ul><li>Dependencies: TASK-021, TASK-022, TASK-023</li><li>Module: <code>services/control.mk</code>, <code>Makefile</code></li><li>Acceptance: <ul><li><code>services/control.mk</code> targets: <code>control-help</code>, <code>control-up</code>, <code>control-down</code>, <code>control-health</code>, <code>control-logs</code></li><li><code>control-up</code>: calls <code>docker network create arc_platform_net 2&gt;/dev/null || true</code>, then <code>heimdall-up</code>, <code>nick-fury-up</code>, <code>mystique-up</code> sequentially</li><li><code>control-down</code>: calls <code>mystique-down</code>, <code>nick-fury-down</code>, <code>heimdall-down</code> (reverse order)</li><li><code>control-health</code>: calls all three health targets; exits non-zero if any fails</li><li><code>control-logs</code>: fans out logs from all three containers with service name prefixes</li><li>Root <code>Makefile</code> additions: <ul><li><code>include services/gateway/heimdall.mk</code></li><li><code>include services/secrets/nick-fury.mk</code></li><li><code>include services/flags/mystique.mk</code></li><li><code>include services/control.mk</code></li></ul></li><li><code>publish-all</code> target updated to include <code>heimdall-build heimdall-publish</code>, <code>nick-fury-build nick-fury-publish</code>, <code>mystique-build mystique-publish</code></li><li><code>make control-help</code> lists control-* targets</li><li><code>make -n control-up</code> dry-run shows correct chain</li></ul></li></ul></li></ul><hr><h2 id="phase-4-ci-cd-parallel" tabindex="-1">Phase 4: CI/CD (parallel) <a class="header-anchor" href="#phase-4-ci-cd-parallel" aria-label="Permalink to &quot;Phase 4: CI/CD (parallel)&quot;">​</a></h2><p>Both workflows are independent and can be implemented concurrently.</p><ul><li><p>[x] [TASK-031] [P] [CI] [P1] Create <code>.github/workflows/control-images.yml</code></p><ul><li>Dependencies: TASK-024</li><li>Module: <code>.github/workflows/control-images.yml</code></li><li>Acceptance: <ul><li>Mirrors <code>data-images.yml</code> structure exactly</li><li><code>on.push.paths</code>: <code>services/gateway/**</code>, <code>services/secrets/**</code>, <code>services/flags/**</code>, <code>.github/workflows/control-images.yml</code></li><li><code>on.pull_request.paths</code>: same service paths</li><li><code>on.workflow_dispatch</code> with <code>mode</code> input (<code>ci</code> / <code>release</code>)</li><li><code>changes</code> job uses <code>dorny/paths-filter@v3</code> with filters <code>heimdall</code>, <code>nick-fury</code>, <code>mystique</code></li><li><code>build-heimdall</code>, <code>build-nick-fury</code>, <code>build-mystique</code> jobs: parallel, each uses <code>_reusable-build.yml</code><ul><li><code>platforms: linux/amd64</code></li><li><code>service-path: services/gateway</code> / <code>services/secrets</code> / <code>services/flags</code></li></ul></li><li><code>security-heimdall</code>, <code>security-nick-fury</code>, <code>security-mystique</code> jobs: run after respective builds; <code>block-on-failure: false</code> in CI</li><li>YAML parses without errors: <code>python3 -c &quot;import yaml,sys; yaml.safe_load(open(&#39;.github/workflows/control-images.yml&#39;))&quot;</code> exits 0</li></ul></li></ul></li><li><p>[x] [TASK-032] [P] [CI] [P1] Create <code>.github/workflows/control-release.yml</code></p><ul><li>Dependencies: TASK-024</li><li>Module: <code>.github/workflows/control-release.yml</code></li><li>Acceptance: <ul><li>Mirrors <code>data-release.yml</code> structure exactly</li><li><code>on.push.tags</code>: <code>control/v*</code></li><li><code>prepare</code> job: derives <code>image-tag</code> (<code>control/v0.1.0</code> → <code>control-v0.1.0</code>), <code>version</code>, <code>prerelease</code> outputs</li><li><code>build-heimdall</code>, <code>build-nick-fury</code>, <code>build-mystique</code>: parallel after <code>prepare</code>; <code>platforms: linux/amd64,linux/arm64</code>; <code>push-image: true</code>, <code>latest-tag: true</code>, <code>image-tag: \${{ needs.prepare.outputs.image-tag }}</code></li><li><code>security-*</code> jobs: <code>block-on-failure: true</code>; <code>create-issues: true</code> (CRITICAL CVEs block release)</li><li><code>release</code> job: image table (arc-gateway, arc-vault, arc-flags); creates GitHub release via <code>softprops/action-gh-release@v2</code></li><li>Release notes include <code>make control-up</code> / <code>make control-health</code> quickstart</li><li>YAML parses without errors</li></ul></li></ul></li></ul><hr><h2 id="phase-5-integration" tabindex="-1">Phase 5: Integration <a class="header-anchor" href="#phase-5-integration" aria-label="Permalink to &quot;Phase 5: Integration&quot;">​</a></h2><ul><li>[x] [TASK-041] [SERVICES] [P1] End-to-end verification — control plane up + health <ul><li>Dependencies: TASK-031, TASK-032</li><li>Module: <code>services/gateway/</code>, <code>services/secrets/</code>, <code>services/flags/</code></li><li>Pre-condition: <code>arc-sql-db</code> and <code>arc-cache</code> must be running (<code>make oracle-up sonic-up</code> first)</li><li>Acceptance: <ul><li><code>docker network create arc_platform_net 2&gt;/dev/null || true</code> runs without error</li><li><code>make control-up</code> exits 0; <code>docker compose ps</code> shows arc-gateway, arc-vault, arc-flags all in <code>healthy</code> state</li><li><code>make control-health</code> exits 0 (all three health probes pass)</li><li><code>make control-down</code> exits 0; all three containers stop; no orphaned containers</li><li>Independent health checks pass: <code>make heimdall-health</code>, <code>make nick-fury-health</code>, <code>make mystique-health</code></li><li><code>curl -sf http://localhost:8090/ping</code> returns <code>OK</code></li><li>Traefik dashboard accessible: <code>curl -sf http://localhost:8090/dashboard/</code> returns HTTP 200</li><li><code>curl -H &quot;X-Vault-Token: arc-dev-token&quot; http://localhost:8200/v1/sys/health</code> returns HTTP 200 with <code>&quot;sealed&quot;:false</code></li><li><code>curl http://localhost:4242/health</code> returns HTTP 200</li><li>All ports bound to <code>127.0.0.1</code>: <code>docker compose ps</code> confirms</li><li>No persistent volumes for Heimdall or Nick Fury: <code>docker volume ls | grep arc</code> shows no <code>arc-gateway-*</code> or <code>arc-vault-*</code></li><li>Heimdall uid verified: <code>docker inspect arc-gateway --format &#39;{{.Config.User}}&#39;</code> returns <code>1000:1000</code> or <code>1000</code> (or root documented)</li><li><code>services/persistence/initdb/002_create_unleash_db.sql</code> exists; Mystique connects to Unleash DB without error</li></ul></li></ul></li></ul><hr><h2 id="phase-6-polish" tabindex="-1">Phase 6: Polish <a class="header-anchor" href="#phase-6-polish" aria-label="Permalink to &quot;Phase 6: Polish&quot;">​</a></h2><ul><li><p>[x] [TASK-900] [P] [DOCS] [P1] Docs &amp; links update</p><ul><li>Dependencies: TASK-041</li><li>Module: <code>services/profiles.yaml</code>, <code>CLAUDE.md</code>, <code>.specify/config.yaml</code></li><li>Acceptance: <ul><li><code>services/profiles.yaml</code> <code>think</code> profile includes <code>heimdall</code> (already done in TASK-001 — verify final state)</li><li><code>services/profiles.yaml</code> <code>reason</code> profile includes <code>nick-fury</code> and <code>mystique</code></li><li><code>CLAUDE.md</code> monorepo layout section references <code>gateway/</code>, <code>secrets/</code>, <code>flags/</code> directories</li><li><code>CLAUDE.md</code> Service Codenames table updated: Nick Fury row shows <code>OpenBao</code> (not <code>Infisical</code>)</li><li><code>.specify/config.yaml</code> <code>secrets</code> service entry: <code>tech</code> changed from <code>infisical</code> to <code>openbao</code></li><li>No broken internal references in modified files</li></ul></li></ul></li><li><p>[x] [TASK-999] [REVIEW] [P1] Reviewer agent verification</p><ul><li>Dependencies: ALL</li><li>Module: all affected modules</li><li>Acceptance (full checklist — reviewer runs all items without needing to open plan.md): <ul><li>All tasks TASK-001 through TASK-900 marked <code>[x]</code> complete</li><li><code>make control-up</code> exits 0 (pre-condition: oracle + sonic running via <code>make oracle-up sonic-up</code>)</li><li><code>docker compose ps</code> shows arc-gateway, arc-vault, arc-flags all in <code>healthy</code> state</li><li><code>make control-health</code> exits 0</li><li><code>make control-down</code> exits 0; no orphaned containers</li><li><code>make heimdall-up &amp;&amp; make heimdall-health</code> works independently</li><li><code>make nick-fury-up &amp;&amp; make nick-fury-health</code> works independently</li><li><code>make mystique-up &amp;&amp; make mystique-health</code> works independently (requires oracle + sonic)</li><li><code>curl http://localhost:8090/ping</code> returns <code>OK</code></li><li><code>curl http://localhost:8090/dashboard/</code> returns HTTP 200</li><li><code>curl -H &quot;X-Vault-Token: arc-dev-token&quot; http://localhost:8200/v1/sys/health</code> returns HTTP 200 with <code>&quot;sealed&quot;:false</code></li><li><code>curl http://localhost:4242/health</code> returns HTTP 200</li><li><code>docker inspect arc-gateway --format &#39;{{.Config.User}}&#39;</code> confirms uid 1000 (or root deviation documented)</li><li><code>docker inspect arc-flags --format &#39;{{.Config.User}}&#39;</code> confirms non-root or deviation documented</li><li>Nick Fury Dockerfile contains comment explaining root deviation + dev-only scope</li><li>All ports bind <code>127.0.0.1</code>: verify with <code>docker compose ps</code></li><li>No persistent volumes for Heimdall or Nick Fury: <code>docker volume ls | grep arc</code> shows no <code>arc-gateway-*</code> or <code>arc-vault-*</code></li><li><code>services/profiles.yaml</code> <code>think</code> includes <code>heimdall</code>; <code>reason</code> includes <code>nick-fury</code> + <code>mystique</code></li><li><code>Makefile</code> includes <code>heimdall.mk</code>, <code>nick-fury.mk</code>, <code>mystique.mk</code>, <code>control.mk</code></li><li><code>publish-all</code> includes <code>heimdall-build heimdall-publish</code>, <code>nick-fury-build nick-fury-publish</code>, <code>mystique-build mystique-publish</code></li><li><code>control-images.yml</code> path filters: <code>services/gateway/**</code>, <code>services/secrets/**</code>, <code>services/flags/**</code></li><li><code>control-release.yml</code> tag format <code>control/v*</code>; platforms <code>linux/amd64,linux/arm64</code></li><li>All three Dockerfiles have OCI (<code>org.opencontainers.*</code>) + <code>arc.service.*</code> labels</li><li>No plaintext credentials in any compose file (env values are dev defaults, not production secrets)</li><li><code>CLAUDE.md</code> Service Codenames table: Nick Fury row shows <code>OpenBao</code> (not <code>Infisical</code>)</li><li><code>.specify/config.yaml</code> <code>secrets</code> service entry: <code>tech: openbao</code></li><li><code>services/persistence/initdb/002_create_unleash_db.sql</code> exists</li><li>Constitution compliance verified: II PASS, III PASS, IV PASS, V PASS, VII PASS, VIII PASS†, XI PASS <ul><li>VIII†: Nick Fury root deviation justified (dev-only, documented)</li></ul></li></ul></li></ul></li></ul><hr><h2 id="progress-summary" tabindex="-1">Progress Summary <a class="header-anchor" href="#progress-summary" aria-label="Permalink to &quot;Progress Summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Phase</th><th>Total</th><th>Done</th><th>Parallel</th></tr></thead><tbody><tr><td>Setup</td><td>1</td><td>1</td><td>0</td></tr><tr><td>Foundational</td><td>3</td><td>3</td><td>3</td></tr><tr><td>Implementation (Make + CI)</td><td>6</td><td>6</td><td>5</td></tr><tr><td>Integration</td><td>1</td><td>1</td><td>0</td></tr><tr><td>Polish</td><td>2</td><td>2</td><td>0</td></tr><tr><td><strong>Total</strong></td><td><strong>13</strong></td><td><strong>13</strong></td><td><strong>8</strong></td></tr></tbody></table>`,28))])}const T=i(p,[["render",h]]);export{b as __pageData,T as default};
