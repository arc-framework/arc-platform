name: arc-platform

services:
  arc-sql-db:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/arc-framework/arc-sql-db:latest
    container_name: arc-sql-db
    environment:
      POSTGRES_USER: arc
      POSTGRES_PASSWORD: arc
      POSTGRES_DB: arc
    ports:
      - "127.0.0.1:5432:5432"   # PostgreSQL client — localhost only
    volumes:
      - oracle-data:/var/lib/postgresql/data
      - ./initdb:/docker-entrypoint-initdb.d:ro   # init SQL runs on fresh DB only
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arc || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - arc_platform_net
    restart: unless-stopped

  # ── postgres_exporter: Prometheus metrics sidecar for arc-sql-db ─────────
  # Exposes PostgreSQL metrics at :9187/metrics — scraped by arc-friday-collector.
  arc-sql-db-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: arc-sql-db-exporter
    environment:
      DATA_SOURCE_NAME: "postgresql://arc:arc@arc-sql-db:5432/arc?sslmode=disable"
    networks:
      - arc_platform_net
    restart: unless-stopped

volumes:
  oracle-data:
    name: arc-sql-db-data

networks:
  arc_platform_net:
    external: true
    name: arc_platform_net
