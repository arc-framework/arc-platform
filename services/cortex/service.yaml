name: arc-cortex
codename: cortex
image: ghcr.io/arc-framework/arc-cortex:latest
version: "0.1.0"
description: "Bootstrap orchestrator — provisions all A.R.C. platform infrastructure at startup"
language: go
port: 8081
health: http://localhost:8081/health
depends_on:
  - flash            # arc-flash (NATS)
  - strange          # arc-strange (Pulsar)
  - sonic            # arc-sonic (Redis)
  - friday-collector # arc-friday-collector (OTEL)
  # TODO: re-add oracle when persistence service lands
